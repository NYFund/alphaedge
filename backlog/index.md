# Backlog 索引

本檔案彙整 `backlog/` 內**所有待辦事項文件**的優先級與完成狀態，作為單一入口。

- 新增待辦 `.md` 時：在下表新增一列。
- 實作進度變動時：同步更新該列的「狀態」與「進度」。
- 項目完成並整份移出 `backlog/`（見 [`manage-backlog` skill §5](../.claude/skills/manage-backlog/SKILL.md#5-完成後的處理)）時：刪除該列。

**狀態圖例**：⬜ 未開始（僅規劃）｜🔄 進行中｜✅ 完成｜⛔ 中斷（需在該文件註明中斷點與恢復下一步）｜⏸ 暫緩

> 文件結構與狀態標記規範見 [`manage-backlog` skill](../.claude/skills/manage-backlog/SKILL.md)。

**優先級（Priority）**：`P0` 最高（阻塞其他項目或已在進行）→ `P3` 最低（長期架構規劃）。各文件內部的階段編號一律寫成 `Phase1-1`，不縮寫成 `P1-1`，避免與優先級混淆。

---

## 待辦清單

| 優先級 | 狀態 | 檔案 | 說明 | 進度 | 相依 |
|:------:|:----:|------|------|------|------|
| P1 | ✅ | [專案優化與改善清單.md](專案優化與改善清單.md) | 2026-09-21 改用 uv、shioaji 升 1.7.5 後的全專案分區健檢（回測、實盤、ETL、工具鏈／文件／前端），只收對照程式碼確認過、且其他 backlog 未記錄的問題，另併入兩項工作的後續改善：① **實盤資料正確性**（測試寫進正式 `tw_trading.db` 且留下 19 筆 `REDUCE_ONLY`、紀錄庫時間欄位只存日期導致 HALT 會被解除、`custom_field` 兩邊不一致使重啟接管與撤單失效、撤單後成交被狀態機丟掉、重建 ticket 清空委託列、重連重複建倉、斷線不重連、當沖回補只試一次、補平待辦不比對部位、parity 覆寫回測報表、`--resync-from-broker` 空旗標）；② 回測正確性（持倉上限加碼判定與實盤相反、無報價開倉單繞過成交檢查、停券表漏跨起始日等）；③ ETL（月營收 latest+1 不補缺月、連續合約 NaN 污染、上櫃自營商合計為 0、總覽表不更新、期貨缺口補不回、期貨 tick 重複寫入等）；④ CI／測試（註解規範閘門不在 CI、不 build 映像、測試建出空資料庫）；⑤ 文件（策略開發 skill 仍教舊契約等）；⑥ 其他 backlog 的過時敘述；⑦ 升版後續評估 | 44 / 44 項 ✅ **全部完成**（Phase1-1~Phase1-13、Phase2-1~Phase2-5、Phase3-1~Phase3-13、Phase4-1~Phase4-4、Phase5-1~Phase5-3、Phase6-1~Phase6-3、Phase7-1~Phase7-3；原記的「45 項」與進度表對不上，現查表內共 44 列，一併更正。2026-09-22：`架構重構與冗餘收斂.md` 的八項過時敘述逐條回寫；模擬環境實連評估 `api.contracts`，結論為不遷移合約解析、改採用券賣資格旗標與期貨最後交易日兩個欄位；2026-09-21：測試隔離正式資料並清理正式紀錄庫；實盤紀錄庫時間欄位存完整時間、watchdog 不再因 aware 時間崩潰；`live_run` 寫入段落名與結束紀錄；`custom_field` 由 OMS 單一產生，重啟接管後撤單可送出；盤中重連不再重複建倉；心跳負責斷線重連；當沖回補失敗會重試並推 CRITICAL；隔日補平截到現有持有量並走風控；parity 單日回測不再覆寫研究用報表與 log；`--resync-from-broker` 以券商部位重建歸屬帳（預設只列計畫、`--confirm-resync` 才寫入）；持倉上限的加碼豁免回測與實盤共用同一份判定；回測查不到報價的開倉單改為拒單；停券表涵蓋回測頭幾天的除權息；`submitted_orders` 日期隨 bar 前進；TICK 回測前視限制寫明；月營收改用差集，缺月補得回來；連續合約展期價差跳過 NaN；台股總覽表同鍵以最新快照覆蓋；配股率未知改為計數而非當成 0；期貨日線與籌碼續跑會補中間缺口；保證金公告解析失敗下次會重抓；`--target all` 不再帶到期貨 tick；普通股篩選不再排除 9960、9962；股期標的池抓空時 target 失敗；權益變動表清單併入當季資產負債表的公司；上櫃減資爬蟲比對回傳區間；期貨籌碼當天未公布不再誤判為被擋；pre-commit 的註解規範閘門進 CI；測試不再建出空資料庫、鎖庫測試不再偶發失敗；前端只列有報表的策略資料夾；CI 建置 Docker 映像並跑前端冒煙；策略開發 skill 與前端文件改寫成現行架構；兩份 backlog 的過時敘述已更正；dev extra 建議改 dependency group、新版 shioaji API 暫不採用）；Phase1-4、Phase1-5 於 2026-09-22 完成（撤單不提前轉終態、撤單後才到的成交照收、跨行程重建不清空委託列）；剩 Phase6-1、Phase7-2（皆可開工）；Phase1-12 為 2026-09-21 做 Phase1-2 時新增 | Phase6-1 依既定順序於 Shioaji 升級完成後施作（已完成）；Phase3-8、Phase6-3 關聯 [台股tick改用TimescaleDB.md](台股tick改用TimescaleDB.md) Phase5-2 |
| P1 | ✅ | [Shioaji升級至1.7.md](Shioaji升級至1.7.md) | shioaji 由 1.3.3 升到 1.7.x（2026-09-21 PyPI 最新 1.7.5）：差異盤點、實盤文件中依賴 1.3.3 行為的結論逐條重驗（`OrderState` 不再是 Enum、`StockOrderCond` 缺 `SBLShort`、合約查詢與 Contract V2 等）、修正 `core/broker/tw/` 與回呼、離線重放與模擬環境實連驗證；不借升版之便加新功能 | 7 / 7 項 ✅（S1、S3、S4、S6 於 2026-09-21；S2、S5、S7 於 2026-09-22 盤中模擬環境實測完成：登入、股票＋期貨委託與撤單、`custom_field` 往返、行情錄製與重放；另修正 `refresh_order_status()` 只刷股票帳號的 bug）；**已全部完成**；2026-09-22 使用者決定**與 [實盤下單架構規劃.md](實盤下單架構規劃.md) 完成時一起移出**（實盤規劃的 1.7.5 重驗註記指向本文件的差異細節） | 改用 uv 管理套件已於 2026-09-21 完成並移出。實盤文件的 1.3.3 結論已全部回寫到 [實盤下單架構規劃.md](實盤下單架構規劃.md)，Phase6-1 借券的版本阻塞已解除。〈另案處理〉項目已移到 [專案優化與改善清單.md](專案優化與改善清單.md) Phase7 |
| P2 | 🔄 | [實盤下單架構規劃.md](實盤下單架構規劃.md) | 以 Shioaji 實作實盤下單：`core/broker/`（券商閘道：登入／CA／限流、合約解析、委託轉換、回報正規化、帳務、即時行情）＋ `core/live/`（OMS 狀態機、風控、對帳、`LiveTrader`、`LiveDataFeed`、盤中事件迴圈、`TradingMode` 狀態機、告警與存活監控、實盤與回測 parity 比對、**多策略歸屬帳與資金額度分配**）＋ `tw_trading.db` 紀錄庫；同一支策略不改寫即可從回測切到模擬／正式環境，涵蓋現股、融券／借券、當沖、零股、指數期貨、股票期貨（融資依 D3） | 38 / 48 項 ✅（Phase0、Phase1、Phase2 全數，2026-09-22 模擬環境冒煙、契約測試與股票／期貨實際成交回報核對完成；Phase3 全數；Phase4-1～4-7（Phase4-2 於 2026-09-22 補上官方開休市日曆）；Phase5-1～5-4；Phase6-2 期貨代號轉換與開平倉別、Phase6-5 送單前期貨保證金檢查，2026-09-22）；⛔ 1 項：Phase7-1（**2026-09-23 演練第 1 天三個段落全部失敗、當日作廢**：模擬環境股票帳戶可用餘額回 0，且 `__unattributed__` 的 6 檔不計入各策略持倉占用，兩者相加使帳戶總權益算成 0，額度檢查於 `prepare()` 拒絕啟動；零委託、零成交、沒跑對帳。**順帶證實模擬環境保留隔夜部位**——帳上 6 檔與 9/22 實測逐檔相同、成本合計 514.89 一字不差，故多日演練的隔夜驗收項目有效。修完兩個缺口才能續跑）；🔄 5 項、Phase4-8、Phase4-9（成本校正程式完成，期貨已驗證一致，剩股票實跑）、Phase6-1（送單前當沖與券源檢查完成，剩模擬環境盤中各送一筆）、Phase5-5（部署設定、SIGTERM 收尾與排程文件完成，剩容器內實跑一個交易日）、Phase6-6（換月兩腿程式完成，剩 2026-10-20 換月日模擬實跑）；⏸ 2 項：Phase6-3（股期：送單路徑與策略都還沒有，與暫緩 S4 同條件）、Phase6-4（盤中零股：下游全以張計、回測不支援，已在送單前拒絕，有策略需要時再開）；⬜ 2 項：Phase7-2、Phase7-4（Phase7-3 舊 Shioaji 工具收斂於 2026-09-22 完成）（2026-09-22 Phase6-2 拆出 Phase6-5 保證金檢查、Phase6-6 換月兩腿，步驟數 45 → 47）。**2026-09-22 模擬環境實測另修兩個 bug**：快照時間偏 8 小時（`Snapshot.ts` 是台北牆上時間當 UTC 編碼）、總權益的持倉成本少乘 1000（張 × 每股價）；期貨成交、委託回報、部位與快照的代號已於 Phase6-2 統一為 `TX202610`，平倉單改送 `Cover`（原本一律 `New`，會開出反向新倉） | **Shioaji 已升到 1.7.5（[Shioaji升級至1.7.md](Shioaji升級至1.7.md)，2026-09-22 完成）：本文件以 1.3.3 驗證的結論已逐條重驗並回寫，Phase6-1 借券的版本阻塞已解除；2026-09-22 模擬環境實測股票＋期貨委託與撤單都通過，Phase2-2～2-8 的冒煙可據此收尾**。**Phase0-3（實盤設定）與 Phase0-4（分層登記）已於 2026-09-19 完成**，**Phase0 與 Phase1 已全數完成**（訂單模型加執行欄位後回歸雙線零變動），**Phase0、Phase1 與 Phase2-1 已完成**（介面定案、`FakeBroker` 可腳本化拒單／部分成交／回報亂序／斷線），**Phase2-2 的程式與單元測試已完成、冒煙待跑**（`scripts/manual/manual_shioaji_login.py` 需真實金鑰，相依 Phase0-1）。該步有兩處偏離原規格：限流器改用**滑動視窗**而非 token bucket（券商規則原文就是「每 N 秒合計 M 次」，固定視窗會放行雙倍突發）；**時鐘偏差檢查拆成三層**——原規劃的「登入後比對券商時戳、超過 5 秒拒絕啟動」做不到，因為登入當下拿得到的券商端時間只有合約檔 `update_date`（日期，沒有秒），改為 `login(receive_window=)` 由伺服器把關秒級、合約檔日期擋「差一天以上」、回報時戳才做 5 秒檢查。另實測確認 `Contract` 的 `reference`／`limit_up`／`limit_down`／`day_trade`／`unit`／`multiplier`／`underlying_code` 等 10 個欄位存在，降低 Phase2-3、Phase4-2、Phase6-1、Phase6-3 的欄位名風險（值仍待冒煙確認）。Phase2-3～2-5 的程式與單元測試亦已完成：讀 shioaji 原始碼確認 **`MultiContract.__getitem__` 查不到回 `None` 而非拋例外**（規劃要防的那個坑），且 `api.Contracts.Stocks[代號]` 一次涵蓋上市櫃、原規劃的「依序查 TSE／OTC」不必做；委託轉換撞到 **Enum 必須依值不依名**（`Action.BUY` vs `Action.Buy`）、**`account=None` 會被 pydantic 拒絕**、以及**價格對齊不能用 `TwStockSpec`**（分層 4，`core.broker` 是 3），改用它內部同一個 `StockUtils.round_to_tick()`。**Phase2-5 的 parser 欄位名取自文件、尚未以實際回報核對**，取值走 `.get()` 故錯了會靜默變成空值，要以錄製模式錄一天再確認。Phase2-6~2-8 亦完成：**帳務欄位名全部離線自 shioaji 的 pydantic 模型核對**（含 `StockPosition.cond` 融資券別、`Margin` 的四個保證金欄位），交割款支援 `Settlement`／`SettlementV1` 兩種形狀且一律依欄位名取值；行情釘住兩個**會讓訊號默默不成立**的陷阱——`volume` 必須取 `total_volume`（否則成交量門檻永遠不達標）、`Snapshot.ts` 是**奈秒**（當成秒會得到 1970 年並一路寫進部位與報表）；`ShioajiBroker` 只做委派與限流，唯一例外是保管 Shioaji 的 `Trade` 物件（撤單改價都要傳回原物件）。Phase3-1 已完成並修掉一個靜默資料遺失：原本的 `INSERT OR REPLACE` 會在**任何** unique 衝突時刪掉舊列，而 `live_order.broker_seqno` 上有唯一索引——兩張單撞到同一個 seqno 時第一張的委託紀錄會直接消失；改為 `ON CONFLICT(<主鍵>) DO UPDATE`，非預期的衝突改為拋 `IntegrityError`。Phase3-2~3-4 亦完成：`OrderManager` 的回報佇列改為**單筆跳過**而非整批中止（一筆狀態矛盾的回報若讓例外上拋，排在後面那些正常的成交會全丟）；風控的金額與曝險類已寫成**模組層級純函式**，連實例都不必建就測得動；**Phase3-4 抽出 `core/execution/order_preprocess.py` 後回歸雙線零變動**，單獨一個 commit。Phase3-5~3-9 亦完成，並撈出一個**會每天發作**的 bug：`rebuild_from_broker()` 走 `PositionManager.open_position()`，而它會檢查餘額——重建時餘額已被部位佔住，部位會被靜默丟掉，然後對帳每天都報「本地兩份紀錄不一致」；已改為重建期間停用餘額檢查。多策略的判定（資金分配、同標的仲裁）已拆成 `core/portfolio/aggregation.py` 的純函式。Phase4-1 完成（`PreOpenQuote` 複查時預測的三個陷阱全部命中：property 要帶 setter、`adj_close` 也要擋、`signal_close` 要自己覆寫），回歸雙線零變動；Phase4-2 的程式完成但**官方休市日曆未做**（規劃允許，已列已知限制——平日只剩券商合約檔一個來源，而合約檔更新是副作用不是契約）。Phase4-3~4-5 亦完成，撈出三個問題：`LiveTrader` 的等待迴圈只看時鐘，**時鐘卡住時段落永遠不結束**（已加獨立於時鐘的保險絲）；**台股尾盤 13:25~13:29 與期貨 13:30~13:44 的時窗根本不重疊**，原本的「合併取交集」會產出「開始晚於結束」的窗＝整段都不送單且不報錯，已改為拒絕啟動並要求分開排程；`BaseStrategy` 缺 `setup_apis()` 的預設實作（`BaseDataFeed.setup()` 一律會呼叫它）。Phase4-6 的主流程亦完成（三份 CSV、D7 殘量、次日補平、日終標記、滑價統計），並撈出一個**啟動就炸**的 bug：`TwStockLiveDataFeed` 漏建 `mrr`／`fs` 兩個 API，用到它們的策略會在 `setup_apis()` 當場 `AttributeError`，而訊息看不出是資料源漏建；已補齊並加測試直接比對兩邊建立的 API 集合。實際費用回填待券商查詢方法核對。Phase4-7 亦完成：推播的三條鐵律（失敗不影響主流程、不阻塞、內容遮蔽）各有測試，`build_notifier()` 因為 `base ↔ telegram_notifier` 的循環被分層檢查抓到而抽成獨立 factory；`scripts/live_watchdog.py` 是**獨立行程**（和 LiveTrader 一起死的心跳等於沒有監控），唯讀開庫、不連券商，且**讀不到紀錄庫本身就推播 CRITICAL**。**Phase4-8（訊號 parity）的程式與單元測試已於 2026-09-20 完成、標 🔄**：`compare()` 為純函式，回測那一半由 `core/live/factory.py` 的 `make_daily_backtest_runner()` 注入（`core/live/report/` 在分層 4，自己 import `core.backtest.factory`（6）就是反向相依）；偏離產出清單一項——回測本來不記錄「送出了哪些委託」，`Backtester` 新增 `submitted_orders` 純記錄清單（收集點在檔數上限與排序之後、成交模擬之前），回歸雙線零變動。缺的是 Phase7-1 五日演練的實測（每天 `UNEXPLAINED` 要歸零）。**Phase5-1（盤中策略契約）亦於 2026-09-20 完成契約與守門、標 🔄**：`BaseStrategy.is_intraday` 預設 False；`is_intraday=True` ＋ `Scale.TICK` 在建立 `Backtester` 時直接拒絕（實盤逐筆拿到長度 1 的 list、TICK 回測一次拿整天，兩邊 list 語意不同而兩邊都跑得完、都不報錯）；兩份 README 補上它與 `enable_intraday` 的並排對照（名字像但完全不同件事）。缺的是逐筆派送本身，要等 Phase5-2 的事件迴圈。**Phase5-2（盤中事件迴圈）的迴圈本體與單元測試亦於 2026-09-20 完成、標 🔄**：三個來源共用一個 queue（分開就得輪詢，而輪詢會讓回報與行情的先後變成不確定的）、心跳由 `queue.get(timeout=)` 逾時產生不另開執行緒、報價過期逐檔判定且沒收過的一律視為過期。**實作時撈到一個規格未提、與 `LiveTrader` 同型的 bug**：迴圈結束條件只看時鐘，時鐘停住就永遠不結束且無錯誤訊息，已加獨立於時鐘的保險絲（寫測試時假時鐘讓測試整個掛住才發現）。缺的是接線（把行情 queue 與回報接進同一個事件 queue、由 `LiveTrader` 驅動），建議與 Phase5-3 一起做。**Phase5-3（斷線重連與重啟恢復）亦於 2026-09-20 完成恢復流程、標 🔄**，並撈到一個**正在生效的安全漏洞**：崩潰重啟會靜默解除 halt——`live_run.account_mode` 只在 `finish_run()` 寫入（崩潰時不會被呼叫，停在 NORMAL），而 `get_last_account_mode()` 只讀已結束的紀錄（崩潰那列 `ended_at IS NULL` 被跳過），兩層一起漏。已改為帳戶層降級即時落地 ＋ 啟動時把未結束的舊紀錄標成 CRASHED（順序：標記要在 `mode_state.load()` 之前）。另補 `ShioajiSession.reconnect()` 的唯一呼叫端 `LiveTrader.ensure_connected()`（它同樣是寫好了零呼叫點，斷線後程式會一路跑到收盤、每次送單都失敗）與 `recover_after_reconnect()`（重新訂閱 → 接管委託 → 對帳）。**Phase5-2、Phase5-3 已於 2026-09-21 接線完成並標 ✅**：行情與回報導進同一個事件 queue（換掉兩個 queue 物件而不是改每個回呼；導向後 `drain_execution_queue()` 一律為空，兩邊都撈會重複且順序錯亂）、`OrderManager` 拆出逐事件的 `apply_event()`、`run()` 依 timing 分派到 `run_intraday()`（接上之前 `--phase intraday` 會安靜地跑成日頻流程）。**行情轉換 `to_tick_quote()` 的欄位全部取自 2026-09-21 實錄**，並撈到兩個規劃文件沒有的陷阱：`simtrade`（試撮＝假成交）與 `intraday_odd`（盤中零股的 volume 單位是股不是張，差 1000 倍）。**Phase5-1 亦標 ✅**：重放器（`core/broker/tw/quote_replay.py`）把錄製的 JSONL 還原成與券商推播同形狀的物件、走實盤那份轉換，「同一份錄製餵兩次訊號一致」已驗；fixture 是實錄抽出的 6 筆真資料。**寫這條測試時先寫出一個假綠燈**——第一版沒設 `live_schedule` 也沒走 `prepare()`，兩次都是空清單卻「通過」，已補 `assert first` 先確認真的有訊號。**Phase5-4（日終強制動作）的判定與回補接線亦於 2026-09-21 完成、標 🔄**：判定全寫成純函式；`CONVERT_TO_MARGIN` 在實盤不存在（改走強制回補）、`RAISE` 改為「先回補再降級」，兩者都回傳告警理由不默默改掉；回補**掛在心跳不是行情上**（行情停了 `on_quote` 就不再被呼叫，而日終回補不能因為沒行情就不做）。接線時發現 `Harness` 沒注入 `build_filled_order`、回補路徑會靜靜做不了事，已補齊。缺的是夜盤日期函式的呼叫端（成交歸屬與行情查詢仍用當下日曆日），且期貨權限未開通前驗不到真實夜盤。下一步是 Phase5-5；**Phase1-1 實測發現鎖定版 shioaji 1.3.3 的 `StockOrderCond` 沒有 `SBLShort`**，借券賣出要升版才送得出去，已寫成測試 ratchet 並影響 Phase2-4、Phase6-1；**Phase0-1 已於 2026-09-21（台北 13:13）完成、標 ✅**：股票與期貨 API 權限都已開通，兩筆測試委託（2330 @ 2215、TXFJ6 @ 42686，皆取跌停價）都被券商受理並撤單。過程中撈到並修掉：**合約檔日期檢查從來沒生效過**（券商回斜線格式）、手動腳本把本機時間標成台北時間、**`ShioajiSession.close()` 把 session token 寫進 log**（`opt(exception=True)` 連區域變數都印）。**委託回報 parser 已以實際推播核對、欄位名全對**（Phase2-5 的一半風險解除），成交回報 parser 仍待一筆會成交的委託才驗得到。**D0~D8 已於 2026-09-19 全部裁示定案**（D8 是 D4 改為多策略後新增，D0 是同日補記的既有前提），Phase4-1、Phase5-1、Phase4-6 的阻塞皆已解除：D1 雙段 `ExecutionTiming`、D3 第一階段不開放融資、D5 `init_capital` 為額度上限、D7 開倉放棄／平倉停損必補（採建議）；**D2 改為逐筆觸發、D4 改為直接支援多策略、D6 改為允許市價單（三條偏離建議）**，受影響的 Phase1-2、Phase3-1~Phase3-6、Phase4-3、Phase4-4、Phase5-1、Phase5-2 已同步改寫。**D4 改為多策略後新增 D8（同標的只允許一支策略持有）與三個步驟**（Phase3-7 歸屬帳、Phase3-8 資金額度分配、Phase3-9 跨策略衝突守門），步驟數 42 → 45，設計見該文件〈多策略：歸屬與衝突〉——單一行程單一 session（限流是帳戶級的）、`live_position_lot` 歸屬帳、`CapitalAllocator` 保留／釋放、風控分帳戶層與策略層。三條偏離各留下已知落差：逐筆觸發**回測側重現不了**（盤中策略的回測只能當量級參考），市價單**回測沒有對應物**（`TwStockFillModel` 是固定 bps）；多策略的 D8 守門會讓**實盤績效低於各策略單跑的回測**，此差異在 parity 歸類為 `CROSS_STRATEGY_BLOCKED`。三者的差距都要在 Phase7-1 與 Phase4-8 量出來。**2026-09-19 另補 D0（策略層不分家：一支策略類別同時供回測與實盤，分界線是 `check_*_signal()` 回傳的訂單清單）**——整份規劃一直以它為前提卻未曾列為裁示項，它正是驗收標準 4 的 parity 比對存在的理由。依 D0 重掃後修正六處**會靜默出錯**的缺口（不新增步驟）：`live_schedule` 與 `bar_execution_order` 矛盾時會默默改掉交易順序（Phase4-1 補啟動檢查）、`is_intraday` 兩邊的報價 list 語意不同（Phase5-1 補回測側守門）、`CapitalAllocator` 啟動檢查的分母應為總權益而非可用資金（持倉續跑必誤判，Phase3-8）、策略降級未釋放資金保留會佔住其他策略額度（Phase3-8、Phase4-3）、`__unattributed__` 仍須參與 D8 守門（Phase3-7、Phase3-9）、parity 新增 `CAPITAL_EXHAUSTED` 類別（Phase4-8）。另記一條已知限制：**帳戶層的組合績效在回測端無法重現**（資金排擠、同標的仲裁、帳戶層降級三者只存在於實盤），故各策略單獨回測的績效不可相加當帳戶預期值。同日另補 Phase0-4 漏登記的 `core.live.attribution`／`core.live.capital_allocator` 分層（未登記＝分層 -1，一寫就會讓 `check_layer_deps.py` 失敗），並把 **Phase3-8、Phase3-9 的「判定」與「讀寫」拆兩個檔**——判定以純函式放 `core/portfolio/aggregation.py`（業界通稱 portfolio aggregation；該套件已於 2026-09-19 隨策略分層重構建立），讓日後的多策略組合回測能重用同一份跨策略仲裁邏輯，避免在跨策略層重演一次 D0 要避免的分家。步驟數仍為 45。Phase3-4 會動到 `Backtester`，須跑回歸雙線且不可重產 baseline；[PostgreSQL遷移計畫.md](PostgreSQL遷移計畫.md) 動工時要把 `tw_trading.db` 列入；Phase6-3 股票期貨與 [暫緩工作彙整.md](暫緩工作彙整.md) S4 同屬股期。**2026-09-19 另補 Phase3-3 的一條設計約束**：金額與曝險類的風控規則（單筆／單日金額、張數口數上限、批次曝險、單一標的占比、委託價偏離）要寫成**純函式**，不綁 `core/live/` 的長駐狀態；頻率、kill switch、`TradingMode`、單日虧損則本來就只屬實盤。理由：回測端不需要事前風控（漲跌停屬市場模擬、方向白名單與 `max_holdings` 屬委託前處理），但日後若要讓回測事前評估「會被風控擋多少」，純判定那幾條能直接重用——綁死了就要再搬一次，`sizing.py` 已經是前車之鑑。不新增步驟。**2026-09-19 實作前再對照程式碼複查一輪**，修正七處落差（不新增步驟，仍為 45）：`PreOpenQuote` 覆寫 OHLC 會撞上 `BaseQuote.__init__` 的直接賦值、且 `signal_close` 在 `adj_close` 非 None 時不讀 `close`（Phase4-1）；**策略層 `TradingMode` 沒有落地**——日頻三個段落是三個獨立行程，開盤段降級的策略到尾盤段會靜默回到 `NORMAL`，新增 `live_strategy_mode` 表（Phase3-1、Phase3-3、Phase4-3、Phase4-5）；「次日補平」待辦缺完成標記會重複送單，新增 `live_pending_action` 表（Phase3-1、Phase4-6）；WAL 與 `synchronous=FULL` 落不到現行 `connect_sqlite()`（Phase3-1 產出補 `core/dao/connection.py`）；Phase0-4 漏登記 `core.live` 套件本身；watchdog 讀不到紀錄庫時要推播而非靜靜退出（Phase4-7）；Phase7-1 的相依與驗收對齊進度表。2026-09-17 立項，同日對照量化交易常見架構健檢後補 Phase4-7（告警與存活監控）、Phase4-8（訊號 parity 比對）與 D7，並修訂 D4 的 `custom_field` 用途 |
| P3 | 🔄 | [滑價模型後續優化.md](滑價模型後續優化.md) | 滑價口徑收斂（2026-09-18 完成並移出）時一併盤點、但刻意不做的五項強化：報表輸出滑價成本統計、台股滑價逐標的設定、tick 級改用 bid/ask 成交、依下單量放大的市場衝擊模型、用實盤成交回填校正滑價參數 | 1 / 5 項 ✅（S1，2026-09-18）；S2~S5 ⬜ | **S1 已完成**：報表新增 `Slippage Cost` 與佔損益比例；實測 `ForeignSellShortDayTradeStrategy` 2024Q1 的滑價吃掉已實現損益的 **42.31%**，且實際價差是名目 10 bps 的 2.4 倍（檔位對齊放大），README 的「檔位會吸收小額 bps」已依實測改寫為「會放大」。**S2 的 D2 已於 2026-09-19 裁示：維持不開工**——三條候選依據（日均成交金額分級、實際買賣價差、S5 實盤成交回填）都還不到可用的程度，解除條件是 tick 搬家完成後改用實際買賣價差，或 S5 累積足夠樣本；S3 相依 [台股tick改用TimescaleDB.md](台股tick改用TimescaleDB.md)；S4 相依校準資料；S5 相依 [實盤下單架構規劃.md](實盤下單架構規劃.md) Phase4-6 並需足夠樣本。**S2~S5 都會改變既有回測結果，須重產 baseline**。2026-09-18 由已結案的 `回測滑價口徑收斂.md` 拆出 |
| P2 | ⬜ | [台股tick改用TimescaleDB.md](台股tick改用TimescaleDB.md) | 台股 tick 落地由 DolphinDB 改為 TimescaleDB：`stock_tick` hypertable（7 天 chunk、依 `stock_id` 壓縮）、`COPY` 以「股票 × 交易日」冪等寫入、`stock_tick_load_log` 取代 `tick_metadata.json`、`StockTickAPI` 改用 ConnectorX 讀取（介面不變）、Google 雲端歷史 CSV 全量匯入、移除台股 tick 的 DolphinDB 程式 | 0 / 16 項（Phase0-1~Phase5-3） | **無前置相依**，Phase4-1（盤點雲端 CSV）可最先做。與 [PostgreSQL遷移計畫.md](PostgreSQL遷移計畫.md) 共用 `postgres` service、driver 與 `core/dao/` 連線入口，不互為前置，先做的建立、後做的沿用；環境變數刻意用 `TICK_DATABASE_URL` 而非 `DATABASE_URL`，避免日頻資料提前被切走。Phase5-2 的期貨 tick DolphinDB 程式去留需使用者裁示。**2026-09-18 裁示**：實作完成後不自動把資料寫進正式資料庫——`data/downloads/tw_stock/tick/` 的 541 檔只是測試素材（開發只取抽樣），全量入庫、雲端歷史匯入與 `--target tick` 的真實更新都等使用者下指令才執行。2026-09-16 立項 |
| P3 | ⬜ | [PostgreSQL遷移計畫.md](PostgreSQL遷移計畫.md) | 由 SQLite3（`tw_stock.db`、`tw_futures.db`）遷移到 PostgreSQL 的分階段實作計畫 | 0 / 16 項（Phase0-1~Phase5-3） | **DAO 資料存取層已完成**（2026-09-16，設計見 [資料存取層](../docs/dev/data-access-layer.md)），**2026-09-16 已依實況重新盤點改動面**：SQLite 專屬語法全在 `core/dao/`（`sqlite_master` 1、`INSERT OR` 4、`SAVEPOINT` 1、`GLOB` 1 處），非測試 `import sqlite3` 只剩 DAO 內 5 檔，另有 21 個 `connect_sqlite()` 連線取得點與 40 個直接連 SQLite 的測試檔；Phase1-2、Phase2-1~Phase2-3 縮減為改寫 `core/dao/` 內部，連線入口沿用 `core/dao/connection.py`。影響面廣，建議在其他重構收斂後再動。命名軸線交接的兩項 schema 收斂（台股表名補 `stock_` 前綴、`stock_id` → `symbol`）排在本批，欄位 Enum 下沉已完成。**2026-09-18 裁示**：實作完成後不自動遷移資料、也不自動切換 backend，Phase3-2 的正式遷移與 Phase5-1 的灰度切換都等使用者下指令 |
| P3 | ⏸ | [暫緩工作彙整.md](暫緩工作彙整.md) | 集中各文件因外部條件暫緩的工作：FinMind 連網冒煙腳本、券商分點 metadata 的 `last_attempted_date` 與 NO_DATA 延遲重試、股票期貨行情前 N 檔回補、平盤下放空限制與每日可當沖清單、漲跌停改用公告值 | 0 / 6 項（S1~S6 ⏸） | **全部暫緩，無可開工項**。S1~S3 等 FinMind 帳號升級（目前 `register`，無券商分點權限，2026-09-16 以真實 API 確認）；S4 等開始開發股期策略，**解除時不可直接跑 `--target futures_stock_price`**，須先暖身（見該文件 S4，做法已於 2026-09-18 依 `resolve_stock_futures_products()` 的 fallback 修正改寫——暖身樣本現在受 `top_n` 約束，不再是整份 320 檔）；S5~S6 等交易所公告資料源盤點（處置／警示股、每日可當沖清單、漲跌停公告值），兩者共用同一批來源，建議同批施作。2026-09-17 由 `DAO重構後續收斂.md`、`券商分點NO_DATA的metadata語意.md`、`爬蟲缺口回補與非交易日批次清理.md` 的暫緩項集中而成；2026-09-18 併入 `docs已載明但未實作的缺口盤點.md` 的 S8、S9（該文件其餘八項已完成並移出）。四份原文件皆已刪除，完成紀錄留在 git 歷史 |
| P3 | 🔄 | [美股ETL與回測架構規劃.md](美股ETL與回測架構規劃.md) | 美股平行模組擴充：`us/` 資料層、provider 抽象、美股回測 model | 1 / 9 項 ✅（Phase3-3，2026-09-02）；**Phase1-1 可直接開工** | **無前置相依**。**2026-09-15 依現行架構改寫目錄落點**：市場軸 `us/` 只開在 `pipeline`／`api`／`adapters`／`backtest/datafeed`；美股策略放 `core/strategies/stock/`、回測 model 放 `core/backtest/models/`，原規劃的 `strategies/us/`、`backtest/engine/`、`backtest/calendars/` 已取消；Phase2-1 改相依 Phase1-3。欄位語言已定案用英文（見 [ETL 入庫約定 §3.4](../docs/pipeline/etl-ingestion.md)）。建議 [PostgreSQL遷移計畫.md](PostgreSQL遷移計畫.md) 先收斂 |
| P3 | ⬜ | [台股新聞情緒溫度計篩選工具.md](台股新聞情緒溫度計篩選工具.md) | 每日爬取台股財經新聞，萃取個股利多/利空情緒並生成可篩選的溫度計指標 | 0 / 7 項（S1~S6 ⬜、S7 ⏸） | 與回測引擎無關，**無外部相依**；但 S1 新聞來源授權檢查阻塞其餘所有步驟，須先做。2026-09-15 補齊產出路徑（ETL 入庫約定檢查表、`core/api/tw/`、研究放 `strategy_lab/data_analysis/news_sentiment/`） |
| P2 | 🔄 | [架構重構與冗餘收斂.md](架構重構與冗餘收斂.md) | 全專案重構盤點後的單一收斂計畫，八個主題：① 共用契約與市場結構搬到中立的 `core/datafeed/`、`core/market/`，反向相依歸零；② `core/adapters/` 收斂成零 I/O 的純轉換層；③ 報價健檢抽成來源無關層，回測（股票＋期貨）與實盤三條路徑共用；④ **以 `BaseDataFeed` 為範本，把另外四個基底宣告成 `@abstractmethod` 的流程骨架收回**；⑤ 拆五個職責混雜的大檔；⑥ 清死碼、修假綠燈文件、補防呆、修兩支護欄腳本、收斂盲捕；⑦ `core/live/` 的結構性收斂；⑧ **回測與實盤共用規則的 parity**（接上 `check_max_holdings`、收斂曝險上限口徑） | 37 / 39 項 ✅（Phase7-1、Phase7-2、Phase7-3、Phase8-1，2026-09-20，**第一批完成**；Phase6-1、Phase6-2，2026-09-22，死碼 12 處刪除、減少 285 行，八處「`--mode live` 尚未實作」的假綠燈敘述改為實況並補退出碼表；Phase6-4 回測模式拒絕實盤旗標；Phase6-3 護欄腳本補上刪除檢查並修掉繫結呼叫的誤判；Phase1-1、Phase1-2 `BaseDataFeed` 與三個市場結構模組搬到中立的 `core/datafeed/`、`core/market/`，反向相依 4 → 1；Phase1-3、Phase1-5 刪掉 `StockUtils` 的死碼，反向相依歸零、ratchet 收尾；Phase1-4 純設定類別抽出，`core.portfolio → core.managers` 消失；Phase4-1、Phase4-2 `BaseDataAPI` 與 `BaseDataLoader` 收回建構骨架；Phase2-1~2-3 adapters 改吃 DataFrame，新增純轉換層閘門；Phase3-1 報價健檢抽成來源無關層；Phase4-4 `BaseLiveDataFeed` 收回骨架；Phase5-1、5-2、5-4 拆 `settlement_model`、`reporter` 抽出繪圖層、`constant.py` 拆七個領域模組；Phase5-5 `Backtester` 刪 5 個包裝、三段重複 docstring 收斂到正本；Phase6-6 測試 helper 收斂：`dao()` 16 份併成 1 份、報價 factory 抽出共用 builder（`make_order` 逐份 diff 後確認不該合併，`make_ticket` 改名消除遮蔽 fixture 的陷阱）；Phase4-3 `DailyTwoMarketUpdater` template method，三支日頻 updater 616 → 250 行，節流上提到基底並接上可中斷中止，`GracefulStop` 使用者 1 → 5 支；Phase5-3 拆 `financial_statement_updater.py`（1,250 → 門面 509 ＋ `financial_statement/equity_change.py` 712），三個 `update_*` 合成 `update_statement(spec)`，並修掉與共用 `throttle()` 同名異簽的遮蔽；Phase3-5 回測／實盤報價口徑守門測試，**提前於 Phase3-2／3-4 施作**以先釘住現行口徑當安全網）；Phase6-5 🔄（規範修正完成、53 處盲捕待排程後施作）；**2026-09-23 新增 Phase4-5**（Phase4-3 之後全庫出現三份同名異簽的 `throttle` 互相遮蔽，其中 `FuturesChipUpdater` 那份仍是裸 `time.sleep()`、按 Ctrl+C 要睡滿；Phase4-3 的守門測試清單漏列它與 `FinancialStatementUpdater`，補齊清單就會當場變紅），步驟數 38 → 39，**同日完成**：三份改名為 `throttle_per_file`／`throttle_per_request`／`throttle_per_window`，`FuturesChipUpdater` 實際有兩處裸 sleep（月批次間隔與被擋重試退避）都改成可中斷；守門測試改用 AST 掃描，並記下三支刻意不列的短跑 updater 與理由；Phase7-5 抽 `RiskEventLogger` 收斂 13 個寫入點，**撈到一個正在發作的 bug**：severity 有 `WARN`／`WARNING` 兩種拼法，後者不是合法的 `NotifyLevel`，保證金不足等四類事件的推播從來沒送出去過；Phase8-2 單一標的曝險上限收斂成一份公式，兩組既有測試（53 條）一行未改就全過＝純抽取，四個面向的差異寫進 docstring 與 14 條新測試；Phase3-2 報價健檢抽成來源無關層，期貨端首次套用（全年實跑 0 筆 warning）、實盤改為無效價略過而非產出 0 元報價，另修好一條被 `slow` 標記藏住的長期紅燈測試；Phase3-3 拆掉兩層 `if scale ==` 分支，日線與 tick 各一條完整路徑（以新舊兩版逐欄位比對證明行為零變更，差異 0 處）；Phase3-4 兩條轉換路徑命名一律 `from_<來源形狀>`（36 處改名、9 個檔案，快照與逐筆推播分開命名），並把「為什麼有兩份」寫進模組說明與 `docs/backtest/multi-market-engine.md`；Phase3-6 `PreOpenQuoteMixin` 收斂盤前報價的六組 property（兩個市場各一份、含三個會靜默失效的陷阱），並釘住「mixin 必須排在 MRO 前面」；Phase7-4 `BaseLiveDataFeed` 收回 DB 生命週期骨架（表名、歷史報價拒絕外殼、連線關閉上移），並修掉**期貨拿股票合約探測交易日**——那是期貨在平日的唯一佐證，兩市場開休市不一致時方向是「誤判為開市」；規格的 `close()` 相似度 1.00 是誤判（期貨多一條 `tw_stock.db` 連線，照表上移會天天洩一條），探測改為可覆寫方法而非字串常數（期貨要商品＋到期月兩個參數，寫死月份會在交割後永遠查不到），並實連模擬環境確認期貨合約確實帶得出 `update_date`；Phase7-6 `core/live/` 零星收斂四件事：**規格的驗收條件用規格的做法達不到**（只移一行 `import shioaji` 仍 ImportError——factory 還在模組層級 import 兩個 broker 類別，而它們自己 import SDK；遞移那條從本檔看不出來），延後兩者後以子行程守門測試釘住；跨日待辦改傳 `PendingAction` dataclass 而非資料列（那個參數會落到策略作者手上，傳 dict 等於把紀錄庫 schema 變成公開契約），**但 DAO 維持回傳資料列**以免建立全專案第一條 `core.dao → core.models` 邊；對帳器補上策略層快照的加權均價（未實現損益刻意不補，這一層沒有報價來源，按口數分攤會得到每列都錯的數字）；並補上 `core/strategies/README.md` 從缺的 `build_cover_order()` 契約文件；Phase7-7 ⏸；其餘 ⬜ | **與 [實盤下單架構規劃.md](實盤下單架構規劃.md) 不是二選一**：Phase7-1~7-3 與 Phase8-1 屬實盤關鍵路徑、須插在該文件 Phase5 之前，其餘 33 步排在等冒煙的空檔（見該文件〈與 `實盤下單架構規劃.md` 的施作順序〉）。**Phase7-1 價值最高**：四道實盤安全檢查的程式與測試都完成、正式路徑零呼叫點，約 25 行接線；**Phase7-2 阻塞期貨上線**。分界逐步驟標明、不重複立項；**Phase1-3 會修掉兩份文件對 `check_shioaji_has_tick()` 的相反指示**。2026-09-20 由 `資料源分層與報價轉換層重構.md`（已併入並刪除）與全專案盤點整合而成。全程要求回歸雙線逐筆零變動。**2026-09-20 施作第一批時新增 Phase7-7（⏸）**：Phase7-1 的四道檢查中，① 資料新鮮度、② 開市判定、④ 額度總量都接上並當場生效（測試替身的無日曆來源與額度超配都被擋下，已修正而非繞過）；**③ 單日虧損的接線是對的但輸入恆為 0**——`prepare()` 時點的帳戶是以原始開倉價重建的（未實現為 0），而 `recover()` 不回放已成交回報（已實現也不在本行程），故 `RiskConfig.daily_loss_ratio` 3% 形同虛設。已拆成 Phase7-7 並暫緩，解除條件是實盤規劃 Phase5 的盤中事件迴圈上線。步驟數 37 → 38。**2026-09-20 另補主題八（Phase8-1、8-2）**：盤點「回測與實盤共用哪些規則」時撈出兩個 parity 缺口——`check_max_holdings()` 放在共用層 `core/execution/order_preprocess.py` 卻只有回測呼叫，**實盤會送出回測擋掉的開倉單**（Phase8-1，與 Phase7-1 同性質：共用層寫好、正式路徑零呼叫點，故同屬第一批）；單一標的曝險上限在 `position_manager.py` 與 `risk_manager.py` 各寫一份，預設值、適用方向、超限行為三處不同（Phase8-2，只收斂公式不統一口徑，統一會改回測結果）。**本文件的過時敘述已於 2026-09-22 依 [專案優化與改善清單.md](專案優化與改善清單.md) Phase6-1 逐條回寫**（`ta` 那一列刪除、死碼清單補 5 列、Phase6-3／6-4／7-6／7-7／8-1 的前提改寫），內容以該日現況為準 |

---

## 維護規則

1. 每次**新增**待辦 `.md`，同步在上表新增一列，並填寫優先級、狀態、說明與相依。
2. 每次**實作**推進（子任務完成、狀態欄變更），同步更新該列的「狀態」與「進度」。
3. 項目完成並整份移出 `backlog/` 時，刪除本表對應那一列（移出方式見 [`manage-backlog` skill §5](../.claude/skills/manage-backlog/SKILL.md#5-完成後的處理)）。
4. 優先級為當下研判結果，可隨需求調整，但調整後必須讓本表與各文件內的優先序敘述一致。
5. **跨文件的相依寫在「相依」欄**：包含前置條件、與哪些步驟建議同批施作、以及該判斷的日期與理由。
6. 本檔只放**通則**（狀態圖例、優先級、待辦清單、維護規則）；各文件內部的施作紀錄與中斷點寫在該文件自身，不要回流到本檔。
