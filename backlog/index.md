# Backlog 索引

本檔案彙整 `backlog/` 內**所有待辦事項文件**的優先級與完成狀態，作為單一入口。

- 新增待辦 `.md` 時：在下表新增一列。
- 實作進度變動時：同步更新該列的「狀態」與「進度」。
- 項目完成並整份移出 `backlog/`（見 [`manage-backlog` skill §5](../.claude/skills/manage-backlog/SKILL.md#5-完成後的處理)）時：刪除該列。

**狀態圖例**：⬜ 未開始（僅規劃）｜🔄 進行中｜✅ 完成｜⛔ 中斷（需在該文件註明中斷點與恢復下一步）｜⏸ 暫緩

> 文件結構與狀態標記規範見 [`manage-backlog` skill](../.claude/skills/manage-backlog/SKILL.md)。

**優先級（Priority）**：`P0` 最高（阻塞其他項目或已在進行）→ `P3` 最低（長期架構規劃）。各文件內部的階段編號一律寫成 `Phase1-1`，不縮寫成 `P1-1`，避免與優先級混淆。

---

## 待辦清單

| 優先級 | 狀態 | 檔案 | 說明 | 進度 | 相依 |
|:------:|:----:|------|------|------|------|
| P1 | ⬜ | [重構後全專案健檢.md](重構後全專案健檢.md) | 2026-09-25 對重構後的全專案做一次掃描（`core`／`tasks`／`scripts`／`strategy_lab`／`frontend`／`tests`／`docs`／工具鏈）。重構本身收得乾淨——零孤兒模組、零無法到達程式碼、零裸 `except`、零未用 import、零 TODO 積欠。要處理的是三類看不見的東西：① **有定義、無執行端**（帳戶總曝險上限、保證金鏈式比對、parity 的兩個類別、公司行動人工補登），文件與設定保證了不存在的防線；② **假綠燈**（一條測試自己設定再自己斷言、ratchet 在指令失敗時無條件通過、`exc_info` 護欄漏掉含 watchdog 的 11 支 loguru 使用者、子行程測試未沙箱化）；③ **重複實作已分岔**（日／夜盤時段四份且兩份寫死、契約代號拆解兩份、`resolve_multiplier` fallback 不同、證交稅公式兩份）。④ **照著文件做會出錯**——`core/` 註解已於 2026-09-24 全庫稽核過，但 README、`docs/` 與 `strategy_lab/` 不在那次範圍內，積水最深：三個跑不動的 API 範例、一個不存在的檔案（`core/strategies/ridge.py`）、一組全錯的回測圖檔名、一個會 `KeyError` 的欄名（`price` 表無 `成交量`）。另含死碼清理與設定工具鏈，共 47 步 | 0 / 47 項 ⬜ | Phase1-3 建議排在 [實盤下單架構規劃.md](實盤下單架構規劃.md) Phase7-1 演練續跑**之前**（未成交目前會落進 `UNEXPLAINED`，而驗收標準是它為 0）；Phase1-1、Phase3-2 會動實盤主流程，須排在演練結束後；**Phase7-1 拆 `trader.py` 要等到 2026-10-20 之後**——換月程式就在該檔，而 [實盤下單架構規劃.md](實盤下單架構規劃.md) Phase6-6 要在 10/20 尾盤段實跑那條路徑，那是全年一次的驗證機會；Phase2-7 建議排在 [回測與實盤入口拆分及架構收斂.md](回測與實盤入口拆分及架構收斂.md) Phase1-6 之前；Phase3-6 建議排在 [PostgreSQL遷移計畫.md](PostgreSQL遷移計畫.md) 之前 |
| P2 | 🔄 | [實盤下單架構規劃.md](實盤下單架構規劃.md) | 以 Shioaji 實作實盤下單：`core/broker/`（券商閘道：登入／CA／限流、合約解析、委託轉換、回報正規化、帳務、即時行情）＋ `core/live/`（OMS 狀態機、風控、對帳、`LiveTrader`、`LiveDataFeed`、盤中事件迴圈、`TradingMode` 狀態機、告警與存活監控、實盤與回測 parity 比對、**多策略歸屬帳與資金額度分配**）＋ `tw_trading.db` 紀錄庫；同一支策略不改寫即可從回測切到模擬／正式環境，涵蓋現股、融券／借券、當沖、零股、指數期貨、股票期貨（融資依 D3） | **40 / 50 項 ✅**（Phase0～Phase3 全數、Phase4-1～4-7、Phase5-1～5-4、Phase6-2／6-5、Phase7-3／7-5／7-6）；🔄 6 項：Phase4-8 parity、Phase4-9 成本校正、Phase5-5 常駐部署、Phase6-1 融資券與當沖、Phase6-6 換月兩腿（以上五項程式與測試完成，缺實跑驗證）、**Phase7-1 五日演練進行中**（9/23 當日作廢、9/24 端到端通過、9/25 休市日六段正常結束，9/29 起續跑）；⏸ 2 項：Phase6-3 股期、Phase6-4 盤中零股（皆等有策略需要）；⬜ 2 項：Phase7-2 上線檢查表、Phase7-4 文件 | Shioaji 已升至 1.7.5（升級工作已完成並移出）。**Phase7-2、Phase7-4 相依 Phase7-1 演練**；Phase6-6 待 2026-10-20 換月日實跑；Phase6-3 與 [暫緩工作彙整.md](暫緩工作彙整.md) S4 同屬股期、同條件。`tw_trading.db` **維持 SQLite、不隨 [PostgreSQL遷移計畫.md](PostgreSQL遷移計畫.md) 遷移**（2026-09-25 定案）。**架構決策（D0~D8）、多策略歸屬與衝突、逐步驟的實作紀錄與已知限制一律見文件本體**，本欄不再重複。 |
| P2 | ⬜ | [回測與實盤入口拆分及架構收斂.md](回測與實盤入口拆分及架構收斂.md) | 2026-09-25 架構健檢：① `run.py` 以 `--mode` 兼任回測與實盤入口，兩者只共用 `--strategy`，已靠 `_live_only_flags_in_use()`、compose 寫死 `--mode live`、延後 import 三處補丁擋誤用 → 拆成 `python -m apps.backtest`／`python -m apps.live`，`run.py` 先當轉發 shim 再刪除，並讓實盤只載入指定策略；② 回測期間與資金寫死在策略實例上 → 新增 `--start`／`--end`／`--capital` 覆寫，parity 單日回測改走同一個介面；③ `core/managers`、策略基底、`core/live/factory.py` 反向 import `core/backtest/models`（成本、商品規格、`FillConfig`）→ 設定搬到 `core/models/`、市場規則下沉到 `core/market/`，並加分層檢查防止回流；④ 多策略組合回測的去留評估 | 0 / 15 項（Phase1-1~Phase1-7、Phase2-1~Phase2-3、Phase3-1~Phase3-4 ⬜；Phase4-1 ⏸） | **Phase1-3、Phase1-6 會動到實盤排程**：避開 [實盤下單架構規劃.md](實盤下單架構規劃.md) Phase7-1 演練的排程時段施作，launchd 要重裝；Phase1-7 刪 shim 前，新入口必須由排程完整跑過至少一個交易日。**Phase3 與 Phase1 互不相依，可以並行**；Phase1～Phase3 每一步都要回歸雙線零變動。**Phase4-1 ⏸**：等 Phase7-1 的 parity `CROSS_STRATEGY_BLOCKED`／`CAPITAL_EXHAUSTED` 統計，呼應該文件〈多策略〉已知限制中「日後要不要補多策略組合回測」的約定 |
| P3 | 🔄 | [滑價模型後續優化.md](滑價模型後續優化.md) | 滑價口徑收斂（2026-09-18 完成並移出）時一併盤點、但刻意不做的五項強化：報表輸出滑價成本統計、台股滑價逐標的設定、tick 級改用 bid/ask 成交、依下單量放大的市場衝擊模型、用實盤成交回填校正滑價參數 | 1 / 5 項 ✅（S1，2026-09-18）；S2~S5 ⬜ | **S1 已完成**：報表新增 `Slippage Cost` 與佔損益比例；實測 `ForeignSellShortDayTradeStrategy` 2024Q1 的滑價吃掉已實現損益的 **42.31%**，且實際價差是名目 10 bps 的 2.4 倍（檔位對齊放大），README 的「檔位會吸收小額 bps」已依實測改寫為「會放大」。**S2 的 D2 已於 2026-09-19 裁示：維持不開工**——三條候選依據（日均成交金額分級、實際買賣價差、S5 實盤成交回填）都還不到可用的程度，解除條件是 tick 搬家完成後改用實際買賣價差，或 S5 累積足夠樣本；S3 相依 [台股tick改用TimescaleDB.md](台股tick改用TimescaleDB.md)；S4 相依校準資料；S5 的前置 [實盤下單架構規劃.md](實盤下單架構規劃.md) Phase4-6 已於 2026-09-20 完成（`LiveReporter.summarize_slippage()` 已產出實際滑價），剩下只差累積樣本到門檻；動工前須先定口徑（換算 bps／跳動點、市價單基準價、改成交量加權）。**S2~S5 都會改變既有回測結果，須重產 baseline**。2026-09-18 由已結案的 `回測滑價口徑收斂.md` 拆出 |
| P2 | ⬜ | [台股tick改用TimescaleDB.md](台股tick改用TimescaleDB.md) | 台股 tick 落地由 DolphinDB 改為 TimescaleDB：`stock_tick` hypertable（7 天 chunk、依 `stock_id` 壓縮）、`COPY` 以「股票 × 交易日」冪等寫入、`stock_tick_load_log` 取代 `tick_metadata.json`、`StockTickAPI` 改用 ConnectorX 讀取（介面不變；DDL、COPY 與查詢統一收進 `core/dao/tw/stock_tick_dao.py`，loader 與 API 只呼叫 DAO）、Google 雲端歷史 CSV 全量匯入、移除台股 tick 的 DolphinDB 程式 | 0 / 16 項（Phase0-1~Phase5-3） | **無前置相依**，Phase4-1（盤點雲端 CSV）可最先做。與 [PostgreSQL遷移計畫.md](PostgreSQL遷移計畫.md) 共用 `postgres` service、driver 與 `core/dao/` 連線入口，不互為前置，先做的建立、後做的沿用；環境變數刻意用 `TICK_DATABASE_URL` 而非 `DATABASE_URL`，避免日頻資料提前被切走。Phase5-2 的期貨 tick DolphinDB 程式去留需使用者裁示。**2026-09-18 裁示**：實作完成後不自動把資料寫進正式資料庫——`data/downloads/tw_stock/tick/` 的 541 檔只是測試素材（開發只取抽樣），全量入庫、雲端歷史匯入與 `--target tick` 的真實更新都等使用者下指令才執行。2026-09-16 立項 |
| P3 | ⬜ | [PostgreSQL遷移計畫.md](PostgreSQL遷移計畫.md) | 由 SQLite3 研究庫（`tw_stock.db`、`tw_futures.db`）遷移到 PostgreSQL 的分階段實作計畫；實盤紀錄庫 `tw_trading.db` 維持 SQLite | 0 / 16 項（Phase0-1~Phase5-3） | **DAO 資料存取層已完成**（2026-09-16，設計見 [資料存取層](../docs/dev/data-access-layer.md)），**2026-09-25 依實盤架構落地後的實況重測改動面**：研究庫的 SQLite 專屬語法全在 `core/dao/`（`sqlite_master` 1、`INSERT OR` 4、`SAVEPOINT` 1、`GLOB` 1、`rowcount` 3 處），研究庫非測試 `import sqlite3` 共 7 檔（DAO 內 6 檔含共用的 `connection.py`，另 `scripts/manual/` 1 檔），另有 16 處 `connect_sqlite()` 研究庫連線取得點（含實盤 DataFeed 唯讀讀研究庫的 3 處）與 56 個直接連 SQLite 的測試檔（其中 13 檔涉及實盤紀錄庫）；Phase1-2、Phase2-1~Phase2-3 縮減為改寫 `core/dao/` 內部，連線入口沿用 `core/dao/connection.py`。**2026-09-25 定案：實盤紀錄庫 `tw_trading.db` 維持 SQLite、不遷移**（實盤行程是唯一寫入者；「先寫 DB 再送單」若搬到網路資料庫，斷線就不能送單），`connect_live_trading()` 與 `LiveTradeDAO` 不變，`get_engine()` 只涵蓋兩個研究庫。影響面廣，建議在其他重構收斂後再動。命名軸線交接的兩項 schema 收斂（台股表名補 `stock_` 前綴、`stock_id` → `symbol`）排在本批，欄位 Enum 下沉已完成。**2026-09-18 裁示**：實作完成後不自動遷移資料、也不自動切換 backend，Phase3-2 的正式遷移與 Phase5-1 的灰度切換都等使用者下指令 |
| P3 | 🔄 | [暫緩工作彙整.md](暫緩工作彙整.md) | 集中各文件因外部條件暫緩的工作：FinMind 連網冒煙腳本、券商分點 metadata 的 `last_attempted_date` 與 NO_DATA 延遲重試、股票期貨行情前 N 檔回補、平盤下放空限制與每日可當沖清單、漲跌停改用公告值；**2026-09-24 併入 `架構重構與冗餘收斂.md` 的七項**（`MarketCalendar` 建構形狀、`ColumnMapping` 泛型化、期貨 TICK 報價轉換、期貨報表的 refused bequest、`LiveReporter` 補績效指標、兩條曝險上限口徑統一、`core/pipeline/` 剩餘盲捕；當時記 32 處，2026-09-25 現查為 38 處） | 0 / 13 項（S13 🔄，其餘 S1~S12 ⏸） | **除 S13 外全部暫緩**。**2026-09-25 S13 完成財報與權益變動那一段**：`financial_statement_crawler.py` 的 3 處盲捕收斂並歸零（送出請求兩處收成 `OSError`、解析表格兩處改用共用常數），順帶修掉**兩處既有的收窄不足**——`pd.read_html("")` 拋的是 lxml 的 `XMLSyntaxError`，它繼承 `SyntaxError` 而不繼承 `ValueError`，於是「HTTP 200 但 body 全空」在權益變動表會逃到逐檔斷路器並計為非預期例外（**連三檔就中止整段回補**），在共用的 `BaseDataCrawler.parse_html_table()` 則直接炸穿整批日更（股價、融資券、籌碼、除權息四支 crawler 共七個呼叫點）；判準已收斂成 `HTML_PARSE_ERRORS` 一份常數。`equity_change.py` 的 2 處複查後**改判不收斂**（逐檔隔離邊界，收窄會破壞隔離）。五個突變各自只打紅對應的測試，另以唯讀探針對真實 MOPS 確認三態語意不變。**處數一律改以 `ruff --select BLE001` 現查**：`core/pipeline` 全區 38 處，S13 剩 tick 23 處與 finmind 7 處（原記 4 處為低報），兩者皆卡驗證成本；另 6 處屬隔離邊界不收斂。S1~S3 等 FinMind 帳號升級（目前 `register`，無券商分點權限，2026-09-16 以真實 API 確認）；S4 等開始開發股期策略，**解除時不可直接跑 `--target futures_stock_price`**，須先暖身（見該文件 S4，做法已於 2026-09-18 依 `resolve_stock_futures_products()` 的 fallback 修正改寫——暖身樣本現在受 `top_n` 約束，不再是整份 320 檔）；S5~S6 等交易所公告資料源盤點（處置／警示股、每日可當沖清單、漲跌停公告值），兩者共用同一批來源，建議同批施作。**2026-09-25 依重構後程式碼複查全文**：S1、S5~S8、S10~S12 的檔案路徑、呼叫端與方法名稱已對齊現況；S11 經使用者裁示**維持暫緩、與實盤 Phase4-8 脫鉤**（parity 比的是逐筆委託、S11 要的是跨多日績效），解除條件改為 Phase7-1 演練通過且累積約 20 個交易日的帳戶快照。2026-09-17 由 `DAO重構後續收斂.md`、`券商分點NO_DATA的metadata語意.md`、`爬蟲缺口回補與非交易日批次清理.md` 的暫緩項集中而成；2026-09-18 併入 `docs已載明但未實作的缺口盤點.md` 的 S8、S9（該文件其餘八項已完成並移出）。四份原文件皆已刪除，完成紀錄留在 git 歷史 |
| P3 | 🔄 | [美股ETL與回測架構規劃.md](美股ETL與回測架構規劃.md) | 美股平行模組擴充：`us/` 資料層、provider 抽象、美股回測 model | 1 / 9 項 ✅（Phase3-3，2026-09-02）；**Phase1-1 可直接開工** | **無前置相依**。**2026-09-15 依現行架構改寫目錄落點、2026-09-25 依架構重構再校正**：市場軸 `us/` 開在 `pipeline`／`api`／`adapters`／`dao`／`market`／`backtest/datafeed` 六處（`broker`、`live/datafeed` 屬實盤，不在範圍內）；SQL 歸 `core/dao/us/`，美股交易日曆放 `core/market/us/`；美股策略放 `core/strategies/stock/`、回測 model 放 `core/backtest/models/`（結算放 `settlement_model/us_stock.py`），交易單位需一併改 `core/portfolio/sizing.py`；原規劃的 `strategies/us/`、`backtest/engine/`、`backtest/calendars/` 已取消；Phase2-1 改相依 Phase1-3。欄位語言已定案用英文（見 [ETL 入庫約定 §3.4](../docs/pipeline/etl-ingestion.md)）。建議 [PostgreSQL遷移計畫.md](PostgreSQL遷移計畫.md) 先收斂 |
| P3 | ⬜ | [台股新聞情緒溫度計篩選工具.md](台股新聞情緒溫度計篩選工具.md) | 每日爬取台股財經新聞，萃取個股利多/利空情緒並生成可篩選的溫度計指標 | 0 / 7 項（S1~S6 ⬜、S7 ⏸） | 與回測引擎無關，**無外部相依**；但 S1 新聞來源授權檢查阻塞其餘所有步驟，須先做。2026-09-15 補齊產出路徑（ETL 入庫約定檢查表、`core/api/tw/`、研究放 `strategy_lab/data_analysis/news_sentiment/`）；2026-09-25 補上資料存取層（`core/dao/tw/` 新聞與溫度彙總表 DAO＋`dao_factory` 測試）、續跑須傳曆日 `calendar_dates`、下一交易日改用 `MarketHolidayAPI` |

---

## 維護規則

1. 每次**新增**待辦 `.md`，同步在上表新增一列，並填寫優先級、狀態、說明與相依。
2. 每次**實作**推進（子任務完成、狀態欄變更），同步更新該列的「狀態」與「進度」。
3. 項目完成並整份移出 `backlog/` 時，刪除本表對應那一列（移出方式見 [`manage-backlog` skill §5](../.claude/skills/manage-backlog/SKILL.md#5-完成後的處理)）。
4. 優先級為當下研判結果，可隨需求調整，但調整後必須讓本表與各文件內的優先序敘述一致。
5. **跨文件的相依寫在「相依」欄**：包含前置條件、與哪些步驟建議同批施作、以及該判斷的日期與理由。
6. 本檔只放**通則**（狀態圖例、優先級、待辦清單、維護規則）；各文件內部的施作紀錄與中斷點寫在該文件自身，不要回流到本檔。
