# 暫緩工作彙整

## Abstract

- **背景／問題**：幾份 backlog 其餘步驟都已完成，只因個別步驟被外部條件卡住而無法移出 `backlog/`，暫緩項散在各文件，很難一眼看出「在等什麼」。2026-09-17 依使用者指示集中到本文件，原文件（`DAO重構後續收斂.md`、`券商分點NO_DATA的metadata語意.md`、`爬蟲缺口回補與非交易日批次清理.md`）一併刪除，完成紀錄留在 git 歷史。2026-09-18 以同一種方式併入 `docs已載明但未實作的缺口盤點.md` 的 S8、S9（本文件的 S5、S6），該文件其餘八項已完成並移出。 2026-09-24 再以同一種方式併入 `架構重構與冗餘收斂.md` 的七項（本文件的 S7~S13），該文件 39 步全數完成並移出。
- **卡住的外部條件**：
  1. **FinMind 帳號等級**：目前是 `register`，沒有券商分點資料集（`TaiwanStockTradingDailyReportSecIdAgg`）權限。2026-09-16 以真實 API 確認，請求會拋 `FinMindPermissionError`；批次更新遇到它會在第一個組合就中止。券商分點相關工作因此都無法實跑，也沒有日更在跑。
  2. **股票期貨策略尚未立項**：股期行情回補要 2.5~9 天的請求量，目前沒有任何策略交易股票期貨。
  3. **美股模組尚未進場**：兩項抽象（欄位對照的泛型化、期貨報表的繼承翻轉）都要有第二個市場或第二份實作對照著看才抽得對，否則抽出來的是猜的。
  4. **改動會變更回測結果、須重產 baseline**：曝險上限口徑統一與報表繼承翻轉都屬此類，不可與純重構混在一起做。
  5. **驗證成本過高或驗不到**：`core/pipeline/` 剩餘盲捕要實跑各來源 updater，而 tick 要連券商抓逐筆、FinMind 受帳號等級限制。
  6. **交易所公告資料源尚未盤點**：處置股／警示股公告、每日可當沖標的清單與漲跌停公告值都還沒有 ETL，端點格式與歷史涵蓋範圍未確認。S5、S6 共用這一批來源，建議同批施作。
- **目標**：每一項都寫清楚暫緩原因與解除條件；條件滿足時可以直接照本文件開工，不必回頭翻已刪除的文件。
- **範圍界線**：只收「已規劃、因外部條件暫緩」的工作，**不分它原本出自哪一份 backlog**。**不收**已裁示不做的 tick 工作（台股 tick 不回補、期貨 tick 不做，2026-09-14／09-15 使用者裁示）——**唯一的例外是 S9**，它收錄的不是 tick 工作本身，而是那項裁示造成的下游後果（期貨逐筆回測做不了），留著是為了讓人知道這個缺口存在，也不收仍可開工的計畫（[台股tick改用TimescaleDB.md](台股tick改用TimescaleDB.md)，DolphinDB 殘留的去留已併入該文件的 Phase5-2）。
- **驗收標準**：各步驟解除條件滿足並完成後標 ✅；全部完成或改判不做後，本文件移出 `backlog/`。

## 進度追蹤表

| 編號 | 步驟名稱 | 產出檔案 | 驗證方式 | 狀態 | 備註／中斷點 |
|------|----------|----------|----------|:----:|--------------|
| S1 | FinMind 連網冒煙檢查腳本 | `scripts/manual/manual_finmind_smoke.py`、`scripts/manual/README.md` | 有 token 時對暫存 DB 跑完 stock_info／broker_info／單一券商分點組合並印出列數；無 token 時清楚提示後結束 | ⏸ | 原 `DAO重構後續收斂.md` S4。2026-09-16 使用者裁示暫緩：腳本最關鍵的券商分點一段無法實跑。解除條件：帳號升級，或確實需要可重複執行的連網檢查 |
| S2 | 券商分點 metadata 增加 `last_attempted_date` | `core/pipeline/tw/updaters/finmind/common.py`（`BrokerTradingMetadataStore`）、`broker_trading_updater.py`、對應測試 | 見 S2 章節的四條驗證 | ⏸ | 原 `券商分點NO_DATA的metadata語意.md` S1，方案已選定（做法一）。解除條件：帳號升級且券商分點回補或日更開始跑，並確認要省 FinMind 額度 |
| S3 | 券商分點 NO_DATA 區間的延遲重試（可選） | 同 S2 | 手動把 `last_attempted_date` 調到 N 天前，重跑時該區間被重新請求 | ⏸ | 原 `券商分點NO_DATA的metadata語意.md` S2。相依 S2；不做也不影響 S2 的效益 |
| S4 | 股票期貨行情：前 N 檔定義與回補 | `core/config/settings.py`、回補執行 | N 檔標的在 `futures_price_daily` 有連續序列 | ⏸ | 原 `爬蟲缺口回補與非交易日批次清理.md` S8。解除條件：開始開發股期策略。**不可直接跑 `--target futures_stock_price`**，須先暖身（見 S4） |
| S5 | 平盤下放空限制與每日可當沖清單：資料源 ＋ 撮合呼叫端 | 新增 ETL（處置／警示股、每日可當沖清單）、`core/models/cost_config.py`、`core/backtest/models/{fill_model,cost_model}.py`、`tests/backtest/test_unimplemented_constraints.py` | 設了 `allow_below_reference=False` 會真的擋單並計數；建構期警告移除 | ⏸ | 原 `docs已載明但未實作的缺口盤點.md` S8。解除條件：完成交易所公告來源盤點。與 S6 同批施作 |
| S6 | 漲跌停改用公告值 | `core/backtest/datafeed/tw/stock_datafeed.py`（`get_price_limit_basis()`）、`core/backtest/models/instrument_spec.py`、`core/backtest/models/settlement_model/tw_stock.py` | 公告值與公式值的差異筆數統計；缺公告值時退回公式版 | ⏸ | 原 `docs已載明但未實作的缺口盤點.md` S9。掛點已存在且已被引擎呼叫，缺的只有資料源。相依同 S5 |
| S7 | `MarketCalendar` 建構形狀向 `FuturesCalendar` 看齊 | `core/market/tw/market_calendar.py` 與呼叫端（`stock_datafeed.py` 與兩支台股策略） | 分層檢查仍 0 違規；回歸雙線零變動 | ⏸ | 原 `架構重構與冗餘收斂.md`〈已知但本份不做〉2。`FuturesCalendar` 已是對的形狀（純建構子收交易日清單 ＋ `from_api()`），`MarketCalendar` 則是一組收 `api` 的 static method。**沒有現存呼叫端受阻**，真要做時以前者為範本；呼叫端含策略檔，見 S7 章節 |
| S8 | `ColumnMapping` 泛型化 | `core/adapters/`、`core/config/schema.py` | 台股與美股兩份對照得出來的共用形狀 | ⏸ | 原〈已知但本份不做〉3。目前只有台股一個市場，**抽象是猜的**。解除條件：[美股ETL與回測架構規劃.md](美股ETL與回測架構規劃.md) 的 `adapters/us/` 真的進來 |
| S9 | 期貨 TICK 級報價轉換 | `core/backtest/datafeed/tw/futures_datafeed.py` | `Scale.TICK` 不再回空 list | ⏸ | 原〈已知但本份不做〉4。`get_quotes()` 目前對 `Scale.TICK` 直接 warning 後回空 list |
| S10 | `FuturesBacktestReporter` 的反向繼承翻轉 | `core/backtest/report/base.py`、`futures_reporter.py` | 台股報表圖檔重產 baseline 後逐項比對 | ⏸ | 原〈已知但本份不做〉6。三處都在「把繼承來的台股行為關掉」＝refused bequest。正解是把繪圖與權益序列放進 `BaseBacktestReporter`（目前 13 個抽象、0 實作），**但會改變台股報表圖檔**。`futures_reporter.py` 的 docstring 已建議排到美股加入時一起做 |
| S11 | `LiveReporter` 補績效指標 | `core/live/report/live_reporter.py` | 實盤日報與回測報表可對讀 | ⏸ | 原〈已知但本份不做〉8。實盤日報沒有 Sharpe／MDD／勝率，無法和回測報表對讀；`core/backtest/analysis/` 是純函式、任何層都可 import。建議與 [實盤下單架構規劃.md](實盤下單架構規劃.md) Phase4-8（parity）同批。**2026-09-25 複查：Phase4-8 已 🔄、`parity_checker.py` 已存在，同批的時機已到，是否解除暫緩待裁示** |
| S12 | 兩條曝險上限的口徑統一 | `core/managers/stock/position_manager.py`、`core/models/cost_config.py`、`core/live/risk/{risk_config,risk_manager}.py`、`core/execution/order_preprocess.py` | 回測重產 baseline 後逐筆比對 | ⏸ | 原〈已知但本份不做〉9。公式已收斂成一份（`order_preprocess.exceeds_symbol_exposure()`）並把四個面向的差異寫進 docstring，**但預設值與適用方向刻意未統一**——統一會改變回測結果，且性質上屬「改變交易邏輯」 |
| S13 | `core/pipeline/` 剩餘盲捕收斂 | `core/pipeline/` 多檔 | 每個檔案改完單獨跑一次該來源的 updater 小區間 | 🔄 | **2026-09-25 完成財報與權益變動那一段**（見 S13 章節）：`financial_statement_crawler.py` 的 3 處盲捕收斂並歸零，另修掉同檔一處**既有的收窄不足**與共用 `BaseDataCrawler.parse_html_table()` 的同一個缺口；`equity_change.py` 的 2 處複查後**改判不收斂**（逐檔隔離邊界）。剩 tick 23 處、finmind 7 處，兩者皆卡驗證成本 |

## S1. FinMind 連網冒煙檢查腳本 ⏸

> **⏸ 暫緩紀錄（2026-09-16）**
> - 暫緩原因：使用者裁示。FinMind 帳號等級為 `register`，無券商分點權限，腳本最關鍵的單一組合寫入無法實跑。
> - 解除條件：帳號升級，或確實需要可重複執行的連網檢查。

- **目的**：補回「真的打 FinMind API、真的寫進 DB」的手動檢查。舊的三支 `scripts/manual/` 腳本因過時已刪除，pytest 只涵蓋離線行為；新腳本不再用 mock 改寫 `core.config`。
  現存的 `scripts/manual/manual_finmind_api.py` 只做**讀取端**（唯讀查 `tw_stock.db`、測 `FinMindAPI` 的 11 個查詢方法，寫成標記 `slow` 的 pytest），
  不打 FinMind API、不寫 DB，與本腳本不重疊；本腳本要不要也寫成 `slow` pytest（比照它）或維持獨立腳本＋結束碼，開工時先決定並寫進 `scripts/manual/README.md`。
- **做法**：新增 `scripts/manual/manual_finmind_smoke.py`：
  - 以 `tempfile` 建暫存 DB 與暫存 downloads，**不動 `sys.modules`、不以 mock 改寫 `core.config`**。
    `FinMindUpdater.__init__()` 目前直接讀模組層級的 `TW_STOCK_DB_PATH`，沒有路徑參數：
    先評估是替它加一個 `db_path: Optional[Path] = None` 參數（改動小、測試也能用），
    還是腳本內組 `FinMindContext`＋`FinMindLoader(conn=connect_sqlite(temp_db))` 自行串流程。
  - 依序執行 `update_stock_info()`、`update_broker_info()`、`broker_trading.update_combination("2330", "1020", 近一週, do_commit=False)` 後 `loader.commit()`，各印出 DAO 查到的列數。
  - 帳號等級不足時，券商分點段會拋 `FinMindPermissionError`，腳本要印出清楚訊息並以非零結束碼結束。
  - 無 `FINMIND_API_TOKEN` 時印出設定方式並以非零結束碼結束；跑完刪除暫存目錄。
  - 注意：`.env` 要以明確路徑 `load_dotenv(<專案根>/.env)` 載入——從 stdin 執行的腳本呼叫無參數的 `load_dotenv()` 會在 `find_dotenv()` 拋 `AssertionError`（2026-09-16 實測）。
- **產出**：`scripts/manual/manual_finmind_smoke.py`、`scripts/manual/README.md`。
- **驗證方式**：有 token 時實跑一次，三個資料集列數皆 > 0；無 token 時結束碼非零且訊息清楚。
- **相依**：無。

## S2. 券商分點 metadata 增加 `last_attempted_date` ⏸

> **⏸ 暫緩紀錄（2026-09-17）**
> - 暫緩原因：這是純效率問題——FinMind 回 NO_DATA 時 metadata 不更新，下次會對同一區間重打。但帳號沒有券商分點權限，目前**沒有回補或日更在跑**，重複請求的實際成本為零。券商分點只回補了 5 家券商、停在 `2026-01-28`。
> - 解除條件：帳號升級、券商分點開始回補或日更，且確認要省 FinMind 額度（或 NO_DATA 重打開始影響日更時間）。

- **目的**：把「有資料到哪一天」與「已請求過到哪一天」拆成兩個欄位，已確認沒有資料的 `(broker_id, stock_id)` 區間不再重複請求。
- **現況（2026-09-15 對照程式）**：
  - metadata 由 `common.py` 的 `BrokerTradingMetadataStore` 讀寫，結構為
    `{broker_id: {stock_id: {earliest_date, latest_date}}}`，內容一律**由 DB 反推**。
    批量更新已不寫 CSV，DB ＋ metadata 是唯一的續跑依據。
  - `BrokerTradingUpdater.update()` 以 `get_existing_dates()` 展開 `earliest_date`～`latest_date`
    判斷要不要請求；`update_combination()` 回 `UpdateStatus.NO_DATA` 時只記 debug log、**不動 metadata**。
  - `refresh_from_database()` 在批次開始、每 `BATCH_UPDATE_METADATA_INTERVAL`（500）個組合、
    quota 耗盡等待前與批次結束時都會呼叫，而且**「覆寫日期範圍」與「清掉 DB 沒有的組合」都在這同一個方法內**
    ——下方第 4、5 條的兩個保留點都要改在這裡，漏改的話新欄位一個批次內就會被清掉好幾次。
- **做法**：每個 `(broker_id, stock_id)` 除了來自 DB 的 `earliest_date` / `latest_date`，再多一個 `last_attempted_date`：

  | 欄位 | 語意 | 來源 |
  |------|------|------|
  | `latest_date` | DB 裡**有資料**到哪一天 | DB |
  | `last_attempted_date` | 已向 API **請求過**到哪一天（含 NO_DATA 區間） | 請求流程 |

  1. **決定請求區間**：起始日 = `max(latest_date + 1, last_attempted_date + 1)`。
  2. **API 有資料並寫入 DB**：照現有流程從 DB 更新 `earliest_date` / `latest_date`，並把本次請求的 `end_date` 寫入 `last_attempted_date`。
  3. **API 回傳 NO_DATA**：不寫 DB，但**要更新 metadata**——把 `last_attempted_date` 設為本次請求的 `end_date`。
     注意 `load()` 有快取、只在 `refresh_from_database()` 寫檔後更新，NO_DATA 的寫入要同時更新快取與檔案。
  4. **從 DB 更新 metadata 時**（`refresh_from_database()` 前半段）：只覆寫 `earliest_date` / `latest_date`，**必須保留** `last_attempted_date`。
  5. **清理 metadata 時**（`refresh_from_database()` 後半段、刪除「DB 不存在的組合」的迴圈）：某組合在 DB 沒有任何一筆、但 metadata 有 `last_attempted_date` 時**不要刪除**——那代表「曾請求過但無資料」，刪掉下次又會從頭請求。

  第 4、5 條是最容易漏的兩處：漏了會讓 NO_DATA 的進度被靜默覆蓋或清掉，症狀是「改完之後 API 用量沒有下降」，但不會有任何錯誤訊息。
  **只有 API 正常回空表才算 NO_DATA**：2026-09-16 起 crawler 對權限不足、連線失敗一律拋例外（`FinMindRequestError`／`FinMindPermissionError`），這些情況**不可**推進 `last_attempted_date`。
- **產出**：`core/pipeline/tw/updaters/finmind/common.py`（`BrokerTradingMetadataStore`）、`core/pipeline/tw/updaters/finmind/broker_trading_updater.py`、新增測試（比照既有的 `tests/test_finmind_broker_trading_batch.py`）。
- **驗證方式**：
  1. 對一組已知無資料的 `(broker_id, stock_id)` 連跑兩次，第二次不再發出 API 請求。
  2. 先寫入 `last_attempted_date`，再觸發一次「從 DB 更新」，該欄位不變。
  3. DB 無資料但 metadata 有 `last_attempted_date` 的組合，在清理後仍存在。
  4. 既有 metadata 檔（沒有 `last_attempted_date` 欄位）可以直接讀入，行為與現在相同。
- **相依**：無。範圍只限券商分點的 metadata 讀寫，不動其他 FinMind 資料集、不改 DB schema、不做爬取並行化。

### 附：方案比較

- **做法一：metadata 增加 `last_attempted_date`（採用，即 S2）**。語意清楚、不會重複打已知無資料的區間；資料若延遲上架可能漏掉，需搭配 S3 或定期 backfill。
- **做法二：NO_DATA 時把 `latest_date` 推到 `end_date`（不採用）**。實作最小，但 `latest_date` 的語意會變成「有資料**或**已檢查到這天」，從 DB 更新時必須取兩者較大值，一旦忘記就是靜默錯誤；且 `refresh_from_database()` 的清理迴圈會刪掉 DB 沒有任何一筆的組合，做法二對「從未有資料」的組合依然無效。
- **業界慣例**：watermark／cursor 在「無資料」時也前進；區分 last_success 與 last_attempted；對 NO_DATA 區間設 N 天後重試或定期 backfill。不建議對同一段已知無資料的區間每次都重打（＝目前行為）。

## S3. 券商分點 NO_DATA 區間的延遲重試（可選） ⏸

> **⏸ 暫緩紀錄（2026-09-17）**：與 S2 相同的外部條件，且相依 S2。

- **目的**：S2 會讓「資料延遲上架」的區間永遠不再被請求；對曾回傳 NO_DATA 的區間在 N 天後再試一次。
- **做法**：新增 `recheck_no_data_after_days`（建議 7／14／30）；若 `(today - last_attempted_date).days >= N`，允許重新請求該區間。
- **產出**：同 S2。
- **驗證方式**：把 `last_attempted_date` 手動調到 N 天前，重跑時該區間被重新請求；未滿 N 天時不請求。
- **相依**：S2。

## S4. 股票期貨行情：前 N 檔定義與回補 ⏸

> **⏸ 暫緩紀錄**
> - 2026-09-13 使用者依成本估算裁示暫緩；2026-09-15 複查確認現階段不需要補爬。
> - 暫緩原因：沒有使用端——`core/strategies/futures/` 只有 `MomentumFuturesStrategy`（預設 TX、日線），沒有任何策略交易股票期貨；回測只從 `futures_stock_universe` 讀股期乘數，該表已有資料，不需要行情。
> - 解除條件：**開始開發股期策略**時再開跑，建議先取小範圍（前 10 檔 × 2020 起，約 2.5 天），有需要再往前延伸。
> - **2026-09-18 補充**：下方做法第 1、2 點已依當日的程式修正改寫。起因是預設的 `python -m tasks.update_db` 曾含 `futures_stock_price` 並踩中 fallback，對 320 檔從 2015 年起回補。當日已完成三件事：`futures_stock_price` 移出 `all`／`no_tick`；`resolve_stock_futures_products()` 的 fallback 由「退回整份標的池」改為「取標的池前 `top_n` 檔」；中斷留下的 CAF 半段行情已刪除（8,500 列、1,730 個中繼檔）。

- **目的**：`futures_stock_universe` 有 640 列快照（個股期貨 498、小型個股期貨 94、ETF 期貨 42、小型 ETF 期貨 6），對應 320 檔商品，但 `futures_price_daily` 只有三檔驗證足跡（CDF 2 日、NYF 2 日、EEF 1 日，2026-08-27~08-28），**回補從未開始**（2026-09-18 一次誤觸留下的 CAF 1,700 日已刪除，表內重新只剩這三檔）。
- **成本估算**：`FuturesPriceUpdater.crawl_and_clean_date()` 每個「商品 × 日」固定打日盤＋夜盤 **2 次請求**（夜盤查無資料也照打），速度約 **6.3 秒/次**。這是由 `settings.py` 註解「六檔歷史回補約需 40 小時」與六檔實際入庫日數（約 2.3 萬次請求）反推，**不是逐筆實測**；updater 的節流常數（每次 3~6 秒、每 50 次休 120 秒）算出來也是同一量級。

  | 範圍 | 請求數 | 預估耗時 |
  |------|-------:|---------:|
  | 前 20 檔 × 2015 起（`STOCK_FUTURES_TOP_N` 現值） | 約 11.4 萬 | 8~9 天 |
  | 前 20 檔 × 2020 起 | 約 6.6 萬 | 約 5 天 |
  | 前 10 檔 × 2020 起 | 約 3.3 萬 | 約 2.5 天 |

- **做法**：
  1. **先暖身，不要直接開跑**：`--target futures_stock_price` 走 `FuturesPriceUpdater.update_stock_futures()` → `resolve_stock_futures_products()`。有 `STOCK_FUTURES_TOP_N`（現值 20）時會呼叫 `FuturesStockUniverseAPI.get_top_liquid_products()`，它要求每檔至少 `min_days=5` 個交易日的日盤行情；表內三檔股期只有 1~2 日，排序結果為空。**此時會取標的池的前 `top_n` 檔當暖身樣本**（2026-09-18 修正後的行為；在那之前是退回整份 320 檔），順序依商品代碼、不是流動性。暖身二擇一：
     - `python -m tasks.update_db --target futures_stock_price --from <近 5 個交易日前>`：20 檔 × 5 日約 200 次請求、20 分鐘左右。樣本是標的池的頭 20 檔（`CAF`、`CBF`、`CCF`…），不保證是量大的那批。
     - 以 `update_stock_futures(products=[...])` 直接指定候選商品（優先於 `top_n`），只對候選清單暖身。**想要暖身樣本貼近真正的高流動性標的就走這條**。
  2. 暖身後確認 `get_top_liquid_products()` 排得出**足夠檔數**（不是只回傳一兩檔——那代表暖身樣本裡只有少數幾檔真的有量，排序沒有代表性），再定 N 與起點（`settings.py` 已定案「股票期貨預設只爬流動性前 N 檔」，理由見該處註解），寫進 `core/config/settings.py` 後開正式回補。開跑前 updater 會把估算的請求量寫進 log，對不上預期就先停。
  3. 逐商品例外隔離已於 2026-09-13 完成（`update()` 跑完才拋 `ProductUpdateError`），上市晚於起點的商品不會中止整批。
- **產出**：`core/config/settings.py` 前 N 檔設定與選取依據的註解；回補執行。
- **驗證方式**：N 檔在 `futures_price_daily` 各有連續序列且對照基準日曆零缺口；抽樣一檔比對除權息日的乘數調整正確。
- **相依**：無。解除時若同時有其他交易所回補在跑，注意共用 TAIFEX 請求節奏，不要同時跑。

## S5. 平盤下放空限制與每日可當沖清單 ⏸

> **⏸ 暫緩紀錄（2026-09-17 立項、2026-09-18 移入本文件）**
> - 暫緩原因：卡資料源。處置股／警示股公告與每日可當沖清單都還沒有 ETL。
> - 解除條件：完成下方的來源盤點，確認端點可爬且有足夠歷史。
> - 出處：原 `docs已載明但未實作的缺口盤點.md` S8，該文件其餘八項已完成並移出。

- **目的**：`ShortConstraint.allow_below_reference` 與 `day_trade_whitelist` 兩個欄位（定義在 `core/models/cost_config.py`）**有定義、沒有撮合呼叫端**——設了不會生效。現行做法是 `StockCostModel.check_unimplemented_constraints()` 在建構時發出警告（至少不靜默），並以 `tests/backtest/test_unimplemented_constraints.py` 釘住這個狀態。後果是回測**高估可放空與可當沖的機會數**。

- **做法**：
  1. **先做來源盤點**（這一步才是阻塞點）：證交所的處置股／警示股公告、每日可當沖標的清單，確認端點格式、歷史涵蓋起點與是否需要逐日爬。歷史不足回測區間的話，本步驟要重新評估值不值得做。
  2. ETL 落地依 [ETL 入庫約定 §四](../docs/pipeline/etl-ingestion.md) 的新增 updater 檢查表，DAO 依 [資料存取層 §七](../docs/dev/data-access-layer.md)。
  3. 撮合端接在 `TwStockFillModel`，拒單計入新的 `event_counts` key（既有 key 不可更名，新增可以）。
  4. 接上之後**移除建構期警告**並改寫 `test_unimplemented_constraints.py`——那條測試的用途是釘住「尚未實作」，實作完就該反過來釘住「會擋單」。
  5. 同步改掉寫著「有定義、無呼叫端」的註解：`core/models/cost_config.py` 的 `ShortConstraint` docstring 與欄位註解、`core/strategies/stock/foreign_sell_short_day_trade_strategy.py` 的對應說明。

- **產出**：新的 crawler／cleaner／loader／updater／DAO 一組、`core/models/cost_config.py`、`core/backtest/models/fill_model.py`、`core/backtest/models/cost_model.py`、對應測試。

- **驗證方式**：SHORT 回歸線新增一組情境（平盤下放空被擋、非當沖清單標的被擋）；既有 12 組情境快照零變動。

- **相依**：來源盤點。與 S6 共用同一批公告資料源，建議同批施作。

## S6. 漲跌停改用公告值 ⏸

> **⏸ 暫緩紀錄（2026-09-17 立項、2026-09-18 移入本文件）**
> - 暫緩原因：同 S5，卡公告值資料源。
> - 解除條件：來源盤點完成；或出現真的依賴漲停判定的策略。
> - 出處：原 `docs已載明但未實作的缺口盤點.md` S9。

- **目的**：`TwStockSpec.get_price_limits()` 以「前收 ±幅度後往內對齊檔位」推算漲跌停，**與交易所公告值多數差一檔**（2026-08-15 實測 23,972 筆，相符率 61.6%）。影響 `FillModel.validate()` 的邊界拒單與 `TwStockSettlementModel.check_limit_up_locked()` 的鎖漲停判定（進而影響 `limit_up_cover_failed` 計數）——也就是放空策略最致命的那個尾部風險的計數會偏。

- **做法**：掛點**已經存在且已被呼叫**（`BaseDataFeed.get_price_limit_basis()`，定義在回測與實盤共用的 `core/datafeed/base.py`；`Backtester` 於每根 bar 推入，除權息日已改用 `dividend` 表的開盤競價基準）。缺的只是公告值本身：把公告的漲跌停價接進同一個掛點，公式版退為 fallback。**不要另開一條路徑**——兩處各算一份漲跌停必然漂移。
  實盤端 `core/live/datafeed/tw/stock_live_datafeed.py` 的同名方法**只有口徑定案、實作同樣還沒接**——docstring 寫「一律用交易所公告值（合約檔的 `reference`）」，但方法本體目前是 `return {}`。本步驟只動回測端；兩邊要接的時候口徑要一致，但**不要把實盤端當成已有的成品去對齊**。

- **產出**：`core/backtest/datafeed/tw/stock_datafeed.py`、`core/backtest/models/instrument_spec.py`、`core/backtest/models/settlement_model/tw_stock.py`（若鎖漲停判定需改讀公告值）、對應測試。

- **驗證方式**：抽一段區間統計公告值與公式值的差異筆數與方向；缺公告值的日期仍能回測。

- **相依**：同 S5 的來源盤點。

---

## S7. `MarketCalendar` 建構形狀向 `FuturesCalendar` 看齊 ⏸

- **目的**：兩個日曆的建構形狀不一致。`FuturesCalendar` 是純建構子收交易日清單、
  另以 `from_api()` classmethod 提供便利入口；`MarketCalendar` 則是一組收 `api` 的
  static method——後者讓 `core/market/` 被迫相依資料存取。

- **做法**：以 `FuturesCalendar` 為範本改寫，呼叫端改傳交易日清單。2026-09-25 對照程式，呼叫端有三處：
  - `core/backtest/datafeed/tw/stock_datafeed.py`（開盤日判定、回補日平移）
  - `core/strategies/stock/momentum_strategy_1.py`（`shift_trading_days()`、`get_last_trading_date()`，並以 `MAX_LOOKBACK_DAYS` 對齊自身窗長）
  - `core/strategies/stock/foreign_sell_short_day_trade_strategy.py`（`shift_trading_days()`）

  **策略直接呼叫 static method**，改形狀要動策略檔，並跑回歸確認結果零變動。
  `core/live/datafeed/calendar.py` 是實盤用的第三份日曆（事前判定，語意不同），**不在本步驟範圍**。

- **產出**：`core/market/tw/market_calendar.py` 與三個生產呼叫端
  （`core/backtest/datafeed/tw/stock_datafeed.py`、`momentum_strategy_1.py`、
  `foreign_sell_short_day_trade_strategy.py`）；另有三個測試檔直接 monkeypatch
  它的 static method，會一起變動（`tests/backtest/test_market_calendar_bounds.py`、
  `tests/backtest/test_short_market_constraints.py`、`tests/live/test_rehearsal_readiness.py`）。

- **驗證方式**：分層檢查仍 0 違規；回歸雙線零變動。

- **不要期待降層**：`check_layer_deps.py` 把 `core.market` 登記在 level 3，理由註解寫著
  「與 `core.api` 同層——日曆的建構子收 API 物件，放更低會變成反向相依」。
  本步驟做完也降不下來——`futures_calendar.py` 與 `futures_margin_config.py` 仍各 import
  一支 `core.api`，而**前者正是本步驟要拿來當範本的 `from_api()`**。
  `futures_margin_config.py` 的模組說明還寫著「放進更低的 `core/models/` ……實測分層檢查
  當場報違規」。本步驟的價值是建構形狀一致，不是降層。

- **相依**：無。**沒有現存呼叫端受阻**，屬架構整潔而非阻塞。

---

## S8. `ColumnMapping` 泛型化 ⏸

- **目的**：欄位名已集中（`core/config/schema.py` 的 `PriceColumn`、`FuturesPriceColumn`、`ChipColumn` 等 Enum），
  由 `core/adapters/tw/{stock,futures}_quote_adapter.py` 轉成報價物件，但沒有抽成跨市場的對照抽象。
  **`ColumnMapping` 是預定名稱，程式碼中尚不存在。**

- **暫緩原因**：目前只有台股一個市場，**抽象是猜的**。等
  [美股ETL與回測架構規劃.md](美股ETL與回測架構規劃.md) 的 `adapters/us/` 真的進來，
  兩份對照著抽才抽得對。

- **做法**：等 `adapters/us/` 落地後，把台股與美股兩份欄位對照並排，抽出真正共用的形狀。
  **在那之前不要先抽**——只有一份實作時抽出來的是猜的。

- **產出**：`core/adapters/` 的共用對照抽象；`core/config/schema.py` 的 Enum 依需要調整。

- **驗證方式**：兩個市場的 adapter 走同一份對照；回歸雙線零變動。

- **相依**：美股 ETL 的 adapters 落地。

---

## S9. 期貨 TICK 級報價轉換 ⏸

- **目的**：`TwFuturesDataFeed.get_quotes()` 對 `Scale.TICK` 直接 warning 後回空 list，
  期貨的逐筆回測因此做不了。

- **做法**：把 `Scale.TICK` 分支接上逐筆來源，轉成與日線同語意的報價物件。

- **產出**：`core/backtest/datafeed/tw/futures_datafeed.py`；讀取側的 API 與 DAO 一組。

- **驗證方式**：`Scale.TICK` 能取得報價且欄位語意與日線一致（成交量單位、OHLC 有無）。

- **相依**：**期貨 tick 已裁示不做（2026-09-15），本步驟因此無限期擱置。**
  收在這裡是要留下那項裁示的下游後果——期貨逐筆回測做不了——**不是**打算做它。

  阻塞比目的段寫的更深：**讀取側完全不存在**，`core/api/` 與 `core/dao/` 沒有任何
  期貨 tick 入口，只有寫入側的 `core/pipeline/tw/updaters/futures_tick_updater.py`
  （Shioaji → DolphinDB），而那支程式的去留還掛在
  [台股tick改用TimescaleDB.md](台股tick改用TimescaleDB.md) 的 Phase5-2 待裁示。
  真要做的話，順序是「先定 tick 儲存方案 → 補讀取側 → 才輪到本步驟」。

---

## S10. `FuturesBacktestReporter` 的反向繼承翻轉 ⏸

- **目的**：`FuturesBacktestReporter(StockBacktestReporter)` 是 refused bequest——
  三處都在「把繼承來的台股行為關掉」（覆寫 `get_adjusted_price()` 回傳原值、
  `self.price = None`、註解自承「收下 `price` 只是為了讓兩個 reporter 的建構簽章一致」）。

- **做法**：把繪圖與權益序列放進 `BaseBacktestReporter`（目前是 13 個抽象方法、
  0 個實作的空殼），讓期貨 reporter 直接繼承它。繪圖已於前置工作抽成 `plotting.py`
  （純搬家、零行為變更），屆時改繼承只是換注入對象。

- **產出**：`core/backtest/report/base.py`、`futures_reporter.py`、`stock_reporter.py`。

- **驗證方式**：台股報表圖檔重產 baseline 後逐項比對；期貨報表零變動。

- **暫緩原因**：**會改變台股報表圖檔、需重產 baseline**。
  `futures_reporter.py` 的 docstring 已建議排到美股加入時一起做。

- **相依**：**兩個條件都要滿足**——重產 baseline 的時機（改動會變更台股報表圖檔），以及第二份實作進來（`futures_reporter.py` 的 docstring 建議排到美股加入時一起做）。Abstract 的卡住條件 #3 與 #4 都列到本步驟，就是因為它同時卡在這兩件事上。

---

## S11. `LiveReporter` 補績效指標 ⏸

- **目的**：實盤日報沒有 Sharpe／MDD／勝率（`grep -rn "performance_metrics" core/live/` → 0 命中），
  無法和回測報表對讀。

- **做法**：沿用 `core/backtest/analysis/performance_metrics.py`，不要另寫一份。
  它只 import `math` 與 `typing`，`check_layer_deps.py` 把它登記在 level 1 並註明
  「任何人都可以 import 它而不會拉進任何相依」——所以 `core/live/`（level 4~5）取用它不違規。

- **產出**：`core/live/report/live_reporter.py`；對應測試。

- **驗證方式**：同一段期間的實盤日報與回測報表，Sharpe／MDD／勝率三項可並排對讀。

- **相依**：建議與 [實盤下單架構規劃.md](實盤下單架構規劃.md) Phase4-8（訊號 parity）同批施作。

> **2026-09-25 複查**：Phase4-8 已 🔄，`core/live/report/parity_checker.py` 已存在（目前只做訊號 diff，未用績效指標）。
> 「同批施作」的時機已到，本步驟不再有外部阻塞；是否解除暫緩、併入 Phase4-8 一起做，待使用者裁示。

---

## S12. 兩條曝險上限的口徑統一 ⏸

- **目的**：回測（`ShortConstraint.max_short_exposure_ratio`）與實盤
  （`RiskConfig.single_symbol_exposure_ratio`）的單一標的曝險上限，
  預設值、適用方向（只管空單 vs 多空皆管）、超限行為三處不同。

- **設定所在**：`ShortConstraint` 在 `core/models/cost_config.py`（預設 `None`＝不限），
  `RiskConfig` 在 `core/live/risk/risk_config.py`（預設 0.25；`RISK_LIMIT_CAPS` 限定可調的上限為 0.50）。

- **已完成的部分**：公式已收斂成一份（`core/execution/order_preprocess.py` 的 `exceeds_symbol_exposure()`），
  四個面向的差異寫在該函式的 docstring——**改任一邊之前先看那份對照表**。

- **暫緩原因**：統一預設值與適用方向**會改變回測結果、須重產 baseline**，
  且性質上屬於「改變交易邏輯」而非重構。

- **做法**：先裁示要統一到哪一邊（預設值、只管空單還是多空皆管、超限是整筆拒絕還是批次截斷），
  再改其中一邊靠過去。**公式不必動**，四個面向的差異表就在
  `exceeds_symbol_exposure()` 的 docstring 裡。

- **產出**：`core/models/cost_config.py`、`core/live/risk/risk_config.py`，
  以及動到的那一側呼叫端。

- **驗證方式**：回測重產 baseline 後逐筆比對；`tests/test_symbol_exposure_parity.py`
  已以 AST 檢查呼叫點並斷言該份 docstring，改動後要同步更新它。

- **相依**：重產 baseline 的時機與一次明確裁示。

---

## S13. `core/pipeline/` 剩餘盲捕收斂 🔄

- **目的**：`except Exception` 在 ETL 場景特別危險——它把「網路逾時」（該重試）與
  「資料 schema 變了」（該中止）混為一談。具名例外階層在
  `core/pipeline/utils/exceptions.py`，`DBError`（`core.dao` 提供的 `sqlite3.Error`
  具名別名）可用於包住 DAO 呼叫的那些。

- **進度**：處數一律以 `uv run ruff check core/pipeline --select BLE001 --statistics`
  現查（2026-09-25 全區共 38 處）。帶 bare `raise` 的不列入——ruff 不計，
  因為它沒有真的吞掉例外。

  | 範圍 | 處數 | 狀態 |
  |---|:--:|---|
  | tick 相關（updater／utils／loader／cleaner／crawler） | 23 | ⏸ 有可用的 tick 測試資料集，或接受以單元測試替代實跑（現況要連券商抓逐筆，本機無 tick 表且會寫入大量資料） |
  | `finmind` 相關 | 7 | ⏸ FinMind 帳號升級（`register` 等級實跑必定失敗、驗不到）。**原記 4 處，2026-09-25 現查為 7**，舊數字低報 |
  | `financial_statement_crawler` | 3 → 0 | ✅ 2026-09-25 收斂完畢 |
  | `equity_change` | 2 | ✅ 2026-09-25 複查**改判不收斂**：逐檔隔離邊界，收窄會破壞隔離 |
  | 其餘來源（期貨行情／保證金／休市日／標的池／`base_updater`） | 6 | ⏸ 同屬隔離邊界，已就地寫明理由 |

### 已完成：財報與權益變動（2026-09-25）

收斂的判準是**把「該重試的傳輸失敗」與「該讓人看的程式錯誤」分開**，
而不是逐一列舉例外名稱：

| 位置 | 原本 | 改為 | 理由 |
|---|---|---|---|
| `_crawl_listing_boards()` 送出請求 | `except Exception` | `except OSError` | `requests` 的例外與作業系統層的 `ConnectionError` 都繼承 `OSError`，一條涵蓋所有傳輸失敗；payload 組錯拋的 `TypeError` 則會現形，不再退化成「整季視為失敗」後每次重跑都以警告收場 |
| `_crawl_listing_boards()` 解析表格 | `except Exception` | `except self.HTML_PARSE_ERRORS` | 見下方「空 body」 |
| `_request_equity_changes()` 送出請求 | `except Exception` | `except OSError` | 就地吞掉會讓「環境壞了」退化成每檔重試三次，逐檔回補的連續例外斷路器永遠不觸發 |
| `_request_equity_changes()` 解析表格 | `except ValueError` | `except self.HTML_PARSE_ERRORS` | **既有的收窄不足，順帶修掉**；見下 |
| `BaseDataCrawler.parse_html_table()` | `except (ValueError, IndexError)` | `except (*cls.HTML_PARSE_ERRORS, IndexError)` | **共用入口有同一個缺口**；見下 |

**「HTTP 200 但 body 全空」是漏接的那一種**：`pd.read_html("")` 拋的是 lxml 的
`XMLSyntaxError`，它繼承 `SyntaxError` 而**不繼承 `ValueError`**（2026-09-25 實測，
其餘壞內容才是 `ValueError`）。權益變動表那一處原本只收 `ValueError`，於是空 body
會逃到逐檔斷路器並計為一次非預期例外——**連三檔就中止整段回補**，而那其實是站方
異常、屬於該重試的那一類。

**同一個缺口也在共用的 `BaseDataCrawler.parse_html_table()`**，而那支是股價、
融資券、籌碼、除權息四支日更 crawler 共七個呼叫點的唯一解析入口：
`looks_like_no_data()` 對空字串明確回 False（刻意不當成休市），於是空 body 會一路
走到 `read_html()` 並讓 `XMLSyntaxError` 炸穿整批，而不是記成該日失敗後重試。
2026-09-25 一併修掉。

判準已收斂成 `BaseDataCrawler.HTML_PARSE_ERRORS` 一份常數，三處共用——
`_crawl_listing_boards()` 的 docstring 原本就寫著「判準只留一份，以免各自演化出
不同的失敗處理」，而漂移已經發生過一次（權益變動表那處就是漏的）。
`parse_html_table()` 另加 `IndexError`（取表格索引用得到，其餘兩處不需要）。

缺解析後端（`ImportError`）**刻意不接**：整季重試永遠不會讓套件自己長回來。

- **`equity_change.py` 複查後改判不收斂**：它們不是待收斂項，是**逐檔隔離邊界**，
  收窄會破壞隔離本身。三處的理由都已寫在各自的 docstring 裡：
  爬取（一頁的問題不該炸掉幾十小時的回補，並以連續例外斷路器補償；該處帶
  bare `raise`，ruff 不計入 BLE001）、
  清洗（同上，與 `BaseDataUpdater.clean_one()` 同一條規則）、
  入庫（單檔失敗不中止整批，跑完由 `update_equity_changes()` 一次拋出）。

- **驗證**：`tests/test_pipeline_error_narrowing.py` 新增 5 條並補強既有 1 條；
  五個突變（把五處各自改回原樣）各自只打紅對應的測試，其中把共用常數
  `HTML_PARSE_ERRORS` 拿掉 `SyntaxError` 會同時打紅三處，證明常數確實共用。
  另以唯讀探針對真實 MOPS 確認三態語意不變（全市場報表 10 張表、有資料 3 張表、
  ETF 回 `[]`）。**未實跑 updater 寫入**——三態語意是 crawler 的契約，
  唯讀探針已涵蓋，逐檔回補那 3 小時 37 分驗不到更多東西。

- **其餘 6 處（期貨行情／保證金／休市日／標的池／`base_updater`）**：同屬隔離邊界，
  已就地寫明理由——第三方拋字面上的 `Exception`（FinMind 的 `request_get()`）、
  外掛邊界（各來源自訂的清洗器）、逐項隔離迴圈（一整條子管線或逐商品／逐年度
  迴圈）。收窄它們會破壞隔離。

- **護欄**：`tests/test_quality_ratchet.py` 已擋住「數量繼續增加」，
  `pyproject.toml` 的處數註記也改為由測試自我驗證。

- **做法**：逐處判斷「該重試的外部失敗」與「該讓人看的程式錯誤」，只收前者。
  具名例外階層在 `core/pipeline/utils/exceptions.py`；HTTP 與解析邊界優先用內建例外
  （`OSError` 涵蓋所有傳輸失敗、`BaseDataCrawler.HTML_PARSE_ERRORS` 涵蓋解析失敗），
  DAO 邊界用 `DBError`。**逐項隔離迴圈不收窄**——收窄會破壞隔離本身。

- **產出**：`core/pipeline/` 的 tick 與 finmind 相關多檔；對應測試併入
  `tests/test_pipeline_error_narrowing.py`。

- **驗證方式**：每個檔案改完單獨跑一次該來源的 updater 小區間；每處收窄各配一個突變測試，
  突變後只打紅對應的那一條。

- **相依**：無技術相依，只等驗證條件。
